# Runtime dependencies — exact pins for reproducible installs.
# Canonical source is pyproject.toml; keep these in sync when bumping versions.
# Install:  python -m pip install -r requirements.txt
# (uv is recommended on developer machines: `uv sync` — see docs/deploy.md)

dash==4.3.0
dash-bootstrap-components==2.0.4
plotly==6.8.0
pandas==3.0.3
openpyxl==3.1.5
# pandas' Parquet engine. Never imported by name, but backend/services/data_store.py
# stores every dataset sheet via df.to_parquet(), which fails at runtime without it.
pyarrow==24.0.0
# Imported directly (app.py: flask, data/compute modules: numpy) rather than only
# reached through dash/pandas — declared so a clean install can't miss them.
flask==3.1.3
numpy==2.5.0
# Retry/backoff around the AI backends (backend/services/ai_common.py).
tenacity==9.1.4
# Committee report packs: reportlab writes the PDF, python-docx the Word file,
# and matplotlib renders the charts once as PNG so both formats show the same
# picture. Pillow is reportlab's image backend.
reportlab==5.0.0
python-docx==1.2.0
matplotlib==3.11.0
pillow==12.2.0
requests==2.34.2
anthropic==0.112.0
python-dotenv==1.2.2
waitress==3.0.2
flask-compress==1.24
sqlalchemy==2.0.51
alembic==1.18.5
psycopg[binary]==3.3.4
flask-login==0.6.3
argon2-cffi==25.1.0

# ---- Phase 1: the API and the analytical store ----
# FastAPI serves every capability to the Next.js frontend; Pydantic defines the
# response contracts the TypeScript client mirrors.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
pydantic-settings==2.15.0
# DuckDB queries the Parquet analytics layer. Imported ONLY by
# backend/data_access/duckdb_source.py, so the engine stays storage-agnostic.
duckdb==1.5.5
# FastAPI's TestClient needs httpx at runtime of the test suite.
httpx==0.28.1

# ---- Part G: regulatory circular ingestion ----
# pypdf reads the page text and the page count out of a circular PDF. Pure
# Python and no system libraries, which matters because the Docker image has
# to build the same way on a laptop and on a bank's runner. Scanned pages
# produce no extractable text and are marked NEEDS_OCR rather than stored as
# empty — see backend/regulatory/extract.py, which refuses to pretend.
pypdf==6.4.0
