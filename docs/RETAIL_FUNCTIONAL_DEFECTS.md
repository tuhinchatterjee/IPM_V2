# Retail full-functionality validation — defects found, fixed and retested

Every defect here was found by driving the running application in a real
browser against the same frontend and backend the retail launcher serves. None
was found by reading code, and none is theoretical: each row states what a user
saw before the fix.

Status values: **FIXED** (code changed, regression test added, journey rerun),
**OPEN** (reproduced, not yet fixed), **BLOCKED** (cannot be tested here).

| ID | Severity | What the user saw | Root cause | Fix | Regression test | Retest |
|---|---|---|---|---|---|---|
