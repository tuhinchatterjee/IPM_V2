# Overnight UAT defect register

Every defect below was found by using the product or by reconciling it against
an independent implementation, not by reading code for style. Each carries a
reproduction, a severity, a root cause, the fix, the regression test that now
holds it, and the journey that was re-run afterwards.

Severity: **P0** a demo-stopper; **P1** a wrong or misleading answer a reader
would act on; **P2** a broken or confusing control; **P3** a cosmetic or
wording defect.

| ID | Sev | What was seen | Root cause | Fix | Regression test | Retest |
|---|---|---|---|---|---|---|
| ON-01 | **P1** | An observed default rate of **100%** and an undefined Gini would be reported for any outcome-based metric, because "the latest month with outcomes" is 2026-07 — where 47 rows carry an outcome and **all 47 are defaults**. A facility that has already defaulted has a known outcome immediately; one that has not is unknown until its window closes. | Scoping a matured cohort on `observed_default_within_window IS NOT NULL` selects the defaults and nothing else on every immature month. | The maturity scope is `performance_window_complete_flag` AND `monitoring_eligible_flag`, which the book states on every row. `latest_matured_period` then resolves to **2025-08**, where all 17,238 eligible rows are observed and 370 defaulted — 2.10%. | `TestTheMaturityRuleIsTheOneTheBookStates` (6 gates, incl. one asserting the rate is not near 100) | metric + lens evaluation |
| ON-02 | P2 | A seeded user rendered in every recipient picker, share dialogue and message header as **"Corporate Credit Manager", team "Corporate Credit"** — in a retail-only product. | `backend/services/demo_users.py` predates the conversion, and nobody renders the file, so it read as dead configuration. It is not: the team and job title are shown wherever a person is named. | Retail Credit Manager, team Retail Credit. | corporate-wording sweep | messaging / sharing UAT |
| ON-03 | **P1** | Every Metrics, Lens and Playbook tile was **empty** — a dash where a number should be, which reads as a figure that happens to be missing today rather than as a feature that cannot work. | All 61 metrics in `backend/metrics/library.py` read the two scorecard-validation extracts, the corporate facility position or the corporate staging table. None is in this deployment. An absent dataset returns *unavailable* rather than raising, so nothing ever failed. | A 46-metric retail library on `retail_facility_month`, served under the retail profile; the corporate definitions retained. Both lenses and three Playbook packs rewritten against it. | `test_ret_metrics_library.py` (76) | lens + playbook evaluation, then browser |
| ON-04 | P2 | A function-backed metric (Gini, AUC, KS, PSI) could not run on **any** dataset without a `matured_flag` column — the projection named it unconditionally and the SELECT failed. | `_run_function` appended `matured_flag` to the column list regardless of the dataset, though its own comment said "when it is present". Invisible while the only such metrics read the one extract that has it. | The column is added only when the governed catalogue says the dataset carries it. | `TestEveryServedMetricComputesAgainstTheRealBook` | 5 discrimination metrics now compute |
| ON-05 | P2 | The Retail Credit Risk lens showed **"Retail Expected Credit Loss 15,952,109" twice**, side by side, with no chart. A tile declared as a trend drew a number. | Every lens tile became a METRIC panel whatever its declared visual, and a metric panel computes one scalar. The `visual` field was carried into the payload and ignored. | A `line` tile is now a chart panel across `reporting_month`, a `bar` tile across `product_label`. The ECL trend draws **25 months in calendar order**, every point matching the oracle. | `TestTheLensesShowSomething`; MET-09 | MET-02, MET-03, MET-09 |
| ON-06 | P2 | **"ECL Coverage 0.77"** with no unit. 0.77% or 77%? Eleven percentage tiles on one lens, none carrying a `%`. | `formatMetric` handled the unit `"percent"`; the catalogue declares `"percentage"`. The unmatched case fell to a default that returns the bare number. | Both spellings render a percentage; `probability` renders as one too (0.0241 → 2.41%, so it can be set beside an observed rate of 2.10%); and an unrecognised unit now keeps its name beside the figure instead of disappearing. The retail library also moved to the vocabulary the rest of the product speaks. | frontend unit tests; MET-10 | MET-10 |
| ON-07 | **P1** | The Metric Catalogue published: *"This deployment has no retail impairment dataset. IFRS 9 staging, ECL and coverage exist for the **corporate book** only."* — beside a lens showing Stage 1/2/3 share and a coverage ratio. Corporate wording, and false. | `UNSUPPORTED` was written against the retired datasets and never re-examined. | A retail unsupported list that states what `retail_facility_month` genuinely cannot do — approval rate, roll rate, cure rate, PSI as a tile, realised LGD — with the reason in each case. The old list is retained for the corporate profile. | MET-05; corporate-wording sweep | MET-05 |
| ON-08 | P2 | A committee pack at `/playbook/packs/1523` had **no Back control at all**. Five Playbook routes, two of them detail screens, and the only way out was the sidebar or the browser's own Back. | The "Back to Playbook" link on the pack screen lives inside the state where the pack could NOT be loaded — so it appeared exactly when there was nothing to go back from. | `BackLink` on the pack, the committee, the committees index and the new-pack form, honouring return context. | MET-08 | MET-07, MET-08 |
| ON-09 | **P1** | The Cockpit had **no Early Warning line at all**, and nothing on screen to say why. Every home-page load fired `/early-warning/signals`, which answered **503 — "CreditProbe is starting up or briefly unavailable. Try again in a moment."** It never becomes available. | The strip reads `corporate_borrower_360`, a dataset this installation does not hold. A permanent failure was wearing a transient one's message, and the strip handled it politely by rendering nothing. | A retail strip on `/retail/early-warning`: customers warned, warnings raised, high-severity count, rules firing, exposure behind them and its share of the book — counts of CUSTOMERS and MONEY, not of alerts. | EWS browser suite | Cockpit home |
| ON-10 | **P1** | "whcih porduct has the higest 30+ dpd rate and did its ecl go up frm july?" was answered *"That is a What-If question… For example: **downgrade them two notches**… **unemployment up one percentage point**"*, with **"Downgrade them one notch."** offered as a clickable follow-up. Corporate rating notches, in the Cockpit of a retail-only product. | Two independent causes. `_TERM_OF_ART` did not mask a **delinquency bucket**, so "30+ DPD" was read as a direction plus a magnitude — a scenario. And the report short-circuit required a correctly spelled interrogative anchored at the start, so one transposed pair of letters in "which" flipped a plain reporting question into a scenario. | DPD buckets are masked as terms of art; the interrogative is matched anywhere and a trailing "?" counts as asking; and the unsized-What-If clarification now offers what the RETAIL engine can run, from the retail profile's own starters. | `test_ret_overnight_filters.py`; CP-CHAT-07 | CP-CHAT-07 |
| ON-11 | **P1** | "aug 2026 personal finance salary transfer stage2 ecl vs jul" returned **SAR 8,012,419 → 8,994,012** — the whole personal-finance book — under a heading reading "PERSONAL FINANCE". The true salary-transfer Stage 2 figure is **1,476,382 → 1,700,845**. The answer was **five times too large** and nothing said the two conditions had been dropped. | `salary_transfer_flag` was not a governed dimension at all. And a two-valued dimension cannot be matched by its VALUE — the values are "True"/"False" and nobody types either. "stage2" also failed because the pattern required whitespace after the noun. | Nineteen policy and state flags plus `origination_vintage` are governed, with the words people type; the vocabulary and the entity matcher both resolve a flag by its PHRASE; "stage2" is read. The question now answers 1,476,382 → 1,700,845. | `test_ret_overnight_filters.py` (39) | CP-CHAT-06 |
| ON-12 | P2 | "Tell me the borrower's employer name for the highest-ECL facility" answered with a grain error about rows per facility, rather than saying the field is not carried. | — | see below | — | — |
| ON-13 | **P1** | *"Stress the retail portfolio"*, *"increase mortgage PD by 20%"*, *"stress the credit card book"* and *"increase personal loan PD by 20%"* were each refused: **"That names a different book. What-If Analysis reads the Corporate IFRS 9 domain only, and the retail, SME and card books are measured on their own scales."** In a retail-only product, on its own subject. | `_ANOTHER_BOOK` lists retail, mortgage, credit card, personal loan, auto loan and both scorecards as *other* books — correct for a corporate installation and exactly inverted for this one. | The guard is reversed under the retail profile: what is a different book here is the corporate, wholesale and SME one, with a note that names this installation's single domain. | `test_ret_overnight_filters.py` routing cases | What-If suite |
| ON-15 | **P1** | **"give me a breakdown by product"** silently applied `application_score_band = A` — resolved out of the **indefinite article**. Any question containing the word "a" was scoped to score band A: correctly computed, wrongly scoped, under a note a reader skims past. | `application_score_band` and `behavioural_score_band` carry the single letters A–E, and the entity matcher looked for each as a bare word. `resolve_dimension_value` refuses a value shorter than four characters; `match_all` did not — two readers of one vocabulary, disagreeing. | A short value matches only where the dimension is NAMED: "score band A", "stage 2", never a bare "A". A named band is still matched. | `TestAFilterNobodyAskedForIsNotInvented` (8) | CP-CHAT-01…09 |
| ON-12 | P2 | "Tell me the borrower's employer name for the highest-ECL facility" was refused with *"CreditProbe read this as a question about one row per facility … but the governed data behind it can only be reported as one row for the whole book."* About the wrong thing, and **false** — the book is keyed one row per facility per month. | "ECL" bound to a measure, "employer name" bound to nothing, and the unbound half was dropped in silence. The plan then aggregated to the whole book and the grain contract — working correctly — refused the plan it was handed. A downstream check wearing an upstream failure. | Identity attributes are checked above the planner and refused by name: *"CreditProbe does not carry an employer name. What the governed book holds about employers is Employer id and Employer sector…"* Narrow by design — one class, party identity, where the answer is always no and never a menu. | `TestAnAbsentIdentityFieldIsDeclaredAbsent` (10) | CP-CHAT-19 |
| ON-14 | **P1** | *"What is the 30+ DPD rate?"* → **"425.0 days of days past due"**. *"Which product has the highest 30+ DPD rate?"* → **"1,260 days of days past due across 4 products"**. *"What is the Stage 3 share of exposure?"* → **"3.00 IFRS 9 stage in Stage 3"** — the literal contents of `ifrs9_stage` reported as a share. Four questions a Head of Retail Risk asks before lunch; four confident wrong numbers. | The Cockpit's planner composes an analysis out of CONCEPTS, and a concept is one governed column. A rate is not a column: it is a numerator, a denominator and a scope. Those live together only in the Metric Catalogue, which the Cockpit never consulted, so the planner summed the nearest column. | A metric route above the planner: a question naming a governed metric whose formula carries a denominator or a governed function is answered by the metric engine — the same definition, scope and arithmetic the lens tile uses, so chat and dashboard agree by construction. Plain sums, cohorts, thresholds, movements and two-period comparisons fall straight through to the planner, which does them better. 30+ DPD is now **1.79%**, highest **Credit Card at 3.70%**, Stage 2 share **5.42%**, coverage **0.77%** — every one matching the oracle. | `test_ret_overnight_metric_route.py` (31) | CP-CHAT-20, 21, 22, 24 |
| ON-16 | **P1** | *"Break ECL down by product"* → **"9,517 facilities where ecl final sar fell, between 2025-08 and 2026-08. The 500 shown are ordered worst first."** The most ordinary way in English to ask for a breakdown, answered as a deterioration cohort. | The literal DOWN direction matches a bare "down", and the concept "ECL" is masked out of the clause before the movement scan — so what the reader saw was "Break     down by product" and read it as an assertion that something fell. | A breakdown idiom's "down" is masked before any direction is matched: `break … down`, `broken down`, `drill down`, `top-down`. Masked rather than removed, so *"break ECL down by product and say which fell"* keeps its "fell". | `TestABreakdownIsNotAFall` (11) | CP-CHAT-23 |
| ON-17 | **P1** | After *"Which product has the highest 30+ DPD rate?"* answered "Credit Card", the next question — *"Break ECL down by product"* — came back as **Credit Card only**: one bar, SAR 3,061,762, under a heading reading BY PRODUCT LABEL. The book is 15,952,109. Found in the browser, in a thread; invisible to a single question. | The guard against exactly this was already written and did not fire. The carried filter is on `product_label`; the reader wrote "product". A field a question groups by was recognised only by the name the warehouse uses. | The field is recognised by every spelling the installation publishes for it, so "by product", "per product family", "the product breakdown" all drop a carried `product_label` restriction. | `TestAFieldTheQuestionGroupsByIsNotInheritedAsAFilter` (10) | CP-CHAT-23 |
| ON-18 | **P1** | Scorecard Validation Intelligence published a **Saudi SME Scorecard** — a third domain, three SME datasets behind it, named in the module's own scope sentence ("the three scorecards this deployment validates: Retail Application, Retail Behaviour and Saudi SME") and returned by `/scorecard-validation/overview`. In a retail-only product. | `backend/scorecard/domains.py` predates the conversion and nobody opened the module during it. | `served_domains()` is profile-aware: two domains under retail. The SME code, its build and its datasets are retained for the installation they are true of. | `TestTheServedScorecardsAreThisInstallationsOwn` | Scorecard Validation UAT |
| ON-19 | **P0** | **Every one of the 48 validation tests answered the same sentence**: *"Retail Application Scorecard has no periods with a realised outcome. is not populated in this deployment."* §6 of the brief is twenty questions long and calls this module central to the Head of Retail Risk and auditor case. It answered none of them. | The models were wired to `retail_application_scorecard_monthly_validation` and two siblings — datasets the retail conversion does not build. Meanwhile `retail_facility_month` carries the score, the predicted PD, the realised outcome, the window-complete flag, and for every characteristic its raw value, bin, weight of evidence and points. And the registry knew about one application scorecard where this installation has **four**, one per product, plus four behavioural. | Eight models bound to the one governed book, each scoped to the product it was fitted on and to `monitoring_eligible_flag`; the maturity rule is `performance_window_complete_flag`; a cohort holds each facility **once**, at its most recent matured observation, across all thirteen closed months (one month gave home finance 21 defaults and refused every discrimination test; the cohort gives it 31 and personal finance 277). The engine learned the retail spellings — `_transformed` for weight of evidence, `_raw` for the raw value, `<prefix>_score_logit` for the stored logit. **40 applicable tests, all 40 measured, and IMPL-REPLICATE reproduces the production score on every row from the published specification.** | `test_ret_overnight_scorecard.py` (33) | SC-01…SC-20 |
| ON-20 | P2 | Both retail scorecards declared `jurisdiction = "United Arab Emirates"`, published on `/scorecard-validation/models` and rendered on the model card. This is a Saudi retail book. | Carried from the pre-conversion registry. | Kingdom of Saudi Arabia, and a gate asserting no model claims otherwise. | `test_the_jurisdiction_is_the_one_this_book_is_in` | model card |
| ON-21 | **P1** | Every test carried a **CBUAE MMS/MMG** article, the coverage report was served under the heading *"CBUAE Model Management Standards and Guidance"*, the report disclaimer said its structure was aligned to it, the workbook had a "CBUAE MMS/MMG-aligned section mapping" sheet and the validation opinion called itself "a CBUAE MMS/MMG-aligned validation opinion". United Arab Emirates supervision, throughout a Saudi product, in the one module an auditor reads. | The module was built against CBUAE and the conversion never reached it. | The nine expectations are generic model-validation practice and were already written in the engine's own words; only the labels were the UAE's. Under the retail profile they are shown as `VE-01…VE-09` under *"Generic model-validation expectations, stated by CreditProbe"*, with a note that this installation carries no approved Regulatory Knowledge Release for the Kingdom and therefore cites no supervisor's text. **No SAMA article number was invented to replace them** — that would be a fabricated citation, which is worse than a wrong one. The internal ids stay as the registry's join key. | `TestTheRegulatoryMappingClaimsNoJurisdictionItHasNot` (4) | regulatory tab, report, workbook |
| ON-22 | P2 | *"For the personal-finance application scorecard, show AUC, Gini and KS"* was answered with a **Gini alone**. *"Which application-score inputs have drifted most?"*, *"Show predicted versus observed by score band"* and four more reached *"Which part of the validation?"* — a clarification, to questions that had said exactly which part. | The reader matched one statistic by its NAME and stopped, and its vocabulary held the names of statistics rather than the questions people ask for them. It also required the product and the kind of scorecard to be adjacent, so "the application-score distribution **for personal finance**" resolved to no model at all. | Several statistics from one category run the category; the product and the kind are read apart and both are required, so an ambiguous question still asks rather than guessing; and the phrasings the question set uses are in the vocabulary. | `TestTheQuestionSetReachesTheRightModel` (11) | SC-01…SC-09 |
| ON-23 | P2 | VAR-SIGN reported **9 of 14 fitted terms carry a sign the data does not support** — under a STRUCTURAL limit whose stated tolerance is zero "because there is no defensible non-zero one". The univariate AUCs behind six of those nine were 0.4497, 0.4723, 0.4479… — coin flips on correlations of 0.02. Nine red structural rows in front of an auditor, off noise. | A sign test with no significance qualification: any AUC below 0.5, by any margin, counted as a disagreement. | A sign is checked only where the characteristic's own univariate separation is distinguishable from none at 95% (Hanley–McNeil standard error, stated in the result and in the limitations). The finding is now **3 of 7 checkable terms**, with the seven unchecked ones named as unchecked rather than as agreeing. | `test_ret_overnight_scorecard.py` | VAR-SIGN |
