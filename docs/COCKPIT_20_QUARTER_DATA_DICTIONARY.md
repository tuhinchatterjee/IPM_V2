# The twenty-quarter Cockpit data dictionary

**Generated** from `backend/cockpit_agentic/fields.py` by `scripts/build_cockpit_agentic_v3_docs.py`. Do not edit by hand: a dictionary that disagrees with the catalogue is a document someone will trust.

- Domain: `corporate_cockpit` — the only runtime business domain the Cockpit can reach.
- Catalogue version: `3.0.0`; data version `3.0.0`.
- Reporting quarters: **20**, 2021Q3 through 2026Q2.
- Declared fields: **751**, of which 108 are the twelve collateral types × nine summary measures and 200 are the ten macro factors × twenty offsets.
- Unexpanded placeholders: **0**. Every generated name is a real column that resolves.

Synthetic demonstration data. Generated by CreditProbe for the Cockpit Agentic V3 demonstration. It describes no real borrower, no real facility and no real bank's book, and no parameter in it is calibrated, validated or approved for any regulatory purpose.

## What the counts are

| | |
|---|---:|
| Relations | 11 |
| Common keys (on every relation) | 24 |
| Columns on the wide facility view | 198 |
| Financial ratios | 40 |
| Rating grades | 19 |
| Qualitative questions | 20 |
| Collateral types | 12 |
| Macro factors | 10 |

## The two time axes

The twenty reporting quarters are historical or current observations of the book, and they are the only reporting periods reachable. Separately, for EACH reporting quarter, the macro block has its own window of twenty positions from `quarter_offset` −4 to +15. That is a second axis, not fifteen more observed quarters.

At the 2026Q2 anchor the macro window runs 2025Q2 through 2030Q1. Across all twenty anchors the union is 39 distinct target quarters — and the reporting calendar is still twenty. A positive offset is a FORECAST made at that anchor, never observed future data.

## The rating scale

Nineteen grades. `rating_rank` 1 is AAA and 19 is C; a LARGER rank is a WEAKER grade, so an improving rating is a falling rank. There is no CCC+, no CCC− and no D. `default_flag` is a separate field and grade C is not mechanically default.

`AAA → AA+ → AA → AA- → A+ → A → A- → BBB+ → BBB → BBB- → BB+ → BB → BB- → B+ → B → B- → CCC → CC → C`

## The forty ratios

These are DATA SEMANTICS — what a stored or derived number means, so a reader can tell whether two of them are comparable. They are not an analysis anything is required to perform, and no code in the answer path consumes them as formulas. Each has a `<name>_source_value` holding the source's own figure where it differs, and a `<name>_status` saying whether it is source, derived, unavailable, invalid because the denominator is zero or negative, or incomparable because the period bases differ.

| # | Field | Definition | Unit |
|---:|---|---|---|
| 1 | `current_ratio` | Current assets / current liabilities. | times |
| 2 | `quick_ratio` | (Eligible cash + short-term investments + net trade receivables) / current liabilities. Eligible cash excludes restricted cash; the exclusions are recorded in quick_ratio_basis. | times |
| 3 | `cash_ratio` | (Eligible cash + short-term investments) / current liabilities. | times |
| 4 | `liquidity_ratio` | The bank's own liquidity ratio, whose definition is mandatory and is carried in liquidity_ratio_basis. Where it is identical to the current or cash ratio it is an ALIAS, not an independent signal. | source_defined |
| 5 | `operating_cash_flow_to_current_liabilities` | Operating cash flow / current liabilities. | times |
| 6 | `working_capital_to_total_assets` | Working capital / total assets. | fraction |
| 7 | `liquid_assets_to_total_assets` | Defined liquid assets / total assets. | fraction |
| 8 | `dscr` | Cash available for debt service / matched debt service due. The bank's own basis is preserved in dscr_basis and is not replaced by a generic EBITDA-over-debt-service formula. | times |
| 9 | `interest_coverage_ratio` | EBIT / gross interest expense. | times |
| 10 | `ebitda_interest_coverage` | EBITDA / gross interest expense. | times |
| 11 | `fixed_charge_coverage_ratio` | Source-defined fixed-charge coverage. The numerator add-backs and the lease and principal treatment are recorded in fixed_charge_coverage_basis. | times |
| 12 | `operating_cash_flow_to_debt` | Operating cash flow / total debt. | times |
| 13 | `free_cash_flow_to_debt_service` | Free cash flow / matched debt service due. | times |
| 14 | `net_debt_to_ebitda` | Net debt / EBITDA on the stated period basis. A negative EBITDA makes this economically uninterpretable and it is flagged rather than hidden. | times |
| 15 | `debt_to_ebitda` | Total debt / EBITDA on the stated period basis. | times |
| 16 | `debt_to_equity` | Total debt / shareholders equity. | times |
| 17 | `liabilities_to_assets` | Total liabilities / total assets. | fraction |
| 18 | `equity_to_assets` | Shareholders equity / total assets. | fraction |
| 19 | `long_term_debt_to_capital` | Long-term debt / (long-term debt + shareholders equity). | fraction |
| 20 | `tangible_net_worth_to_debt` | Tangible net worth / total debt. | times |
| 21 | `gross_profit_margin` | Gross profit / revenue. | fraction |
| 22 | `ebitda_margin` | EBITDA / revenue. | fraction |
| 23 | `operating_profit_margin` | EBIT / revenue. | fraction |
| 24 | `net_profit_margin` | Net profit / revenue. | fraction |
| 25 | `return_on_assets` | Net profit / average total assets, using the true opening balance. A PERIOD return unless explicitly annualized, which is stated in ratio_period_basis. | fraction |
| 26 | `return_on_equity` | Net profit / average shareholders equity, on the same period rule. | fraction |
| 27 | `return_on_capital_employed` | EBIT / average source-defined capital employed. | fraction |
| 28 | `operating_cash_flow_margin` | Operating cash flow / revenue. | fraction |
| 29 | `free_cash_flow_margin` | Free cash flow / revenue. | fraction |
| 30 | `total_asset_turnover` | Revenue / average total assets. | times_per_period |
| 31 | `fixed_asset_turnover` | Revenue / average net PPE. | times_per_period |
| 32 | `working_capital_turnover` | Revenue / average working capital. | times_per_period |
| 33 | `inventory_turnover` | Cost of goods sold / average inventory. | times_per_period |
| 34 | `receivables_turnover` | Credit sales / average net trade receivables. Substituting total revenue for credit sales is a DIFFERENT ratio and is only permitted with an explicit alternate basis recorded. | times_per_period |
| 35 | `payables_turnover` | Credit purchases / average trade payables. Substituting cost of sales is likewise a different ratio requiring an explicit basis. | times_per_period |
| 36 | `receivables_days` | Average net receivables / credit sales x matched period days. | days |
| 37 | `inventory_days` | Average inventory / cost of goods sold x matched period days. | days |
| 38 | `payables_days` | Average trade payables / credit purchases x matched period days. | days |
| 39 | `cash_conversion_cycle_days` | Receivables days + inventory days - payables days, all on the same period and basis. | days |
| 40 | `capex_to_operating_cash_flow` | Capital expenditure / operating cash flow. | times |

## The twenty qualitative questions

Observed assessment data. Neither model invents an answer, and the Cockpit does not turn them into a credit score.

| ID | Field | Question |
|---|---|---|
| Q01 | `q01_management_experience_answer` | How experienced is the management team in this business and sector? |
| Q02 | `q02_management_stability_answer` | How stable has senior management been during the assessment period? |
| Q03 | `q03_succession_planning_answer` | Is a documented and credible succession plan in place? |
| Q04 | `q04_governance_oversight_answer` | How effective are board oversight and governance arrangements? |
| Q05 | `q05_ownership_transparency_answer` | How transparent and stable are ownership and control? |
| Q06 | `q06_financial_reporting_quality_answer` | How reliable, timely and complete is financial reporting? |
| Q07 | `q07_audit_issues_resolution_answer` | Are audit qualifications or material audit issues present, and how are they being resolved? |
| Q08 | `q08_strategy_execution_answer` | How clear is the strategy and how well has management executed it? |
| Q09 | `q09_business_model_resilience_answer` | How resilient is the business model to changes in demand and operating conditions? |
| Q10 | `q10_competitive_position_answer` | What is the recorded assessment of the borrower's competitive position? |
| Q11 | `q11_customer_concentration_answer` | How diversified is the customer base and how material is dependence on major customers? |
| Q12 | `q12_supplier_concentration_answer` | How diversified are suppliers and how resilient are supply arrangements? |
| Q13 | `q13_pricing_power_answer` | What pricing power or ability to pass through cost increases is recorded? |
| Q14 | `q14_funding_access_answer` | How reliable and diversified is access to funding? |
| Q15 | `q15_shareholder_support_answer` | What is the recorded capacity and willingness of shareholders to provide support? |
| Q16 | `q16_operational_capacity_answer` | How adequate are production or service capacity, maintenance and operating capabilities? |
| Q17 | `q17_internal_risk_controls_answer` | How effective are internal controls and risk-management practices? |
| Q18 | `q18_legal_regulatory_compliance_answer` | What material legal or regulatory compliance issues are recorded? |
| Q19 | `q19_environmental_social_exposure_answer` | What material environmental, social or climate-related exposures are recorded? |
| Q20 | `q20_business_continuity_answer` | How adequate are business-continuity and key operational-resilience arrangements? |

## The ten macro factors

| # | Factor | Meaning | Unit |
|---:|---|---|---|
| 1 | `real_gdp_growth_yoy` | Real GDP growth, year on year. | percent |
| 2 | `cpi_inflation_yoy` | Consumer price inflation, year on year. | percent |
| 3 | `unemployment_rate` | Unemployment rate on the source population definition. | percent |
| 4 | `policy_interest_rate` | The relevant central-bank policy rate. | percent_per_annum |
| 5 | `interbank_rate_3m` | The relevant three-month interbank or reference lending rate. | percent_per_annum |
| 6 | `sovereign_bond_yield_10y` | The relevant ten-year sovereign bond yield. | percent_per_annum |
| 7 | `fx_lcy_per_usd` | Local-currency units per USD. The direction is fixed: a RISE is a depreciation of the local currency. | lcy_per_usd |
| 8 | `benchmark_oil_price` | The configured benchmark oil price. | usd_per_barrel |
| 9 | `commercial_property_price_index` | Commercial-property price index. Its base period and geography are required to interpret a level. | index |
| 10 | `private_sector_credit_growth_yoy` | Domestic private-sector credit growth, year on year. | percent |

## Joins, and what they multiply

| From | To | On | Cardinality | Warning |
|---|---|---|---|---|
| `cockpit_facility_quarter` | `cockpit_borrower_financial_quarter` | `borrower_id`, `reporting_quarter` | many_to_one | Many facilities share one borrower statement. Aggregate facilities to the borrower first, or the statement is counted once per facility. |
| `cockpit_facility_quarter` | `cockpit_rating_ratio_quarter` | `borrower_id`, `reporting_quarter` | many_to_one | Same repetition as the statements. |
| `cockpit_facility_quarter` | `cockpit_qualitative_quarter` | `borrower_id`, `reporting_quarter` | many_to_many | Twenty question rows per borrower-quarter. This join multiplies every facility row by twenty. |
| `cockpit_facility_quarter` | `cockpit_ifrs9_detail` | `facility_id`, `position_id`, `reporting_quarter` | one_to_many | One row per stored run, scenario and term point. Summing scenario_ecl across scenarios is not the booked ECL; use the stored weights, and do not count scenarios as facilities. |
| `cockpit_facility_quarter` | `cockpit_collateral_allocation` | `facility_id`, `position_id`, `reporting_quarter` | one_to_many | One row per allocated asset. |
| `cockpit_collateral_allocation` | `cockpit_collateral_quarter` | `collateral_id`, `reporting_quarter` | many_to_one | An asset securing three facilities has three allocation rows and ONE asset row. Sum allocated_gross_value_rcy across allocations; summing gross_market_value_rcy triples it. |
| `cockpit_facility_quarter` | `cockpit_covenant_quarter` | `facility_id`, `reporting_quarter` | one_to_many | Facility-scope covenants only. A borrower-wide covenant (binding_scope='borrower') must be joined on borrower_id and counted once per borrower, not once per facility. |
| `cockpit_facility_quarter` | `cockpit_macro_quarter_window` | `reporting_quarter`, `country_code=country_or_region` | one_to_many | Twenty offsets x ten factors per anchor. Filter to the offsets and factors you need, or every facility row is multiplied by two hundred. |

## Every relation, field by field

### `cockpit_reporting_calendar` — Reporting calendar

**Grain:** dataset_release_id x reporting_quarter

29 columns.

| Column | Type | Unit | Aggregation | Definition |
|---|---|---|---|---|
| `tenant_id` | string | — | identifier | Authenticated data owner. Enforced by the server and the query principal; never trusted from model text and never a filter Opus must remember to write. |
| `domain_id` | string | — | identifier | Constant 'corporate_cockpit' for every business artifact this runtime may reach. |
| `dataset_release_id` | string | — | identifier | Immutable release selection, pinned for the whole user request. Two releases are never mixed in one answer. |
| `reporting_quarter` | string | — | point_in_time | One of the twenty authorized anchor quarter labels, e.g. '2026Q2'. |
| `quarter_end_date` | date | — | point_in_time | Calendar end date of the reporting quarter. |
| `data_cutoff_at` | timestamp | — | point_in_time | Latest information timestamp permitted for this snapshot. Nothing published after it was knowable at it. |
| `source_system` | string | — | identifier | Actual source system, or the labelled synthetic generator. |
| `source_record_id` | string | — | identifier | Stable source-record identity, masked where required. |
| `source_period_start` | date | — | point_in_time | True start of the observed or financial-statement period. Not a guessed reporting date. |
| `source_period_end` | date | — | point_in_time | True end of the observed or financial-statement period. |
| `source_published_at` | timestamp | — | point_in_time | When the source published this observation. |
| `source_available_at` | timestamp | — | point_in_time | When this observation became available to the bank. Point-in-time control: an observation is only usable at a snapshot whose cutoff is at or after this. |
| `source_version` | string | — | identifier | Source definition version behind this value. |
| `mapping_version` | string | — | identifier | Transformation/mapping version applied on ingestion. |
| `ingested_at` | timestamp | — | point_in_time | Ingestion timestamp. |
| `provenance_id` | string | — | identifier | Permission-scoped lineage reference for this row. |
| `value_origin` | string | — | enum | How the value came to be. These are never conflated: a carried-forward annual statement is not a newly observed quarterly one, and a forecast is not an actual. |
| `missing_reason` | string | — | enum | Why a value is absent. Absent is not zero, and 'not_applicable' is not 'unknown'. |
| `currency_code` | string | — | enum | Original currency of the source amount. |
| `reporting_currency` | string | — | enum | The release's reporting currency. 'RCY' in a column name means this. |
| `fx_to_reporting_currency` | float | ratio | not_additive | Conversion scalar recorded for this snapshot. A stored scalar, not access to a separate FX domain. |
| `amount_scale` | string | — | enum | Unit, thousand, million. Normalized on ingestion; the source convention is preserved here. |
| `record_status` | string | — | enum | Whether this row is available, partial or not applicable. |
| `observation_age_days` | integer | days | not_additive | Age in days of the genuine observation behind this row, at the snapshot date. |
| `slot_index` | integer | — | ordinal | Position of this quarter among the twenty, 0 through 19. |
| `is_populated` | boolean | — | enum | Whether this slot actually carries observations. Twenty slots created is not twenty quarters observed. |
| `facility_row_count` | integer | count | not_additive | Facility positions observed in this slot. |
| `borrower_row_count` | integer | count | not_additive | Borrowers observed in this slot. |
| `coverage_note` | string | — | not_additive | Why a slot is empty or partial, where it is. |

### `cockpit_facility_quarter` — Facility position × reporting quarter (the wide view)

**Grain:** tenant_id x dataset_release_id x reporting_quarter x facility_id x position_id -- one atomic facility position per reporting quarter

198 columns.

| Column | Type | Unit | Aggregation | Definition |
|---|---|---|---|---|
| `tenant_id` | string | — | identifier | Authenticated data owner. Enforced by the server and the query principal; never trusted from model text and never a filter Opus must remember to write. |
| `domain_id` | string | — | identifier | Constant 'corporate_cockpit' for every business artifact this runtime may reach. |
| `dataset_release_id` | string | — | identifier | Immutable release selection, pinned for the whole user request. Two releases are never mixed in one answer. |
| `reporting_quarter` | string | — | point_in_time | One of the twenty authorized anchor quarter labels, e.g. '2026Q2'. |
| `quarter_end_date` | date | — | point_in_time | Calendar end date of the reporting quarter. |
| `data_cutoff_at` | timestamp | — | point_in_time | Latest information timestamp permitted for this snapshot. Nothing published after it was knowable at it. |
| `source_system` | string | — | identifier | Actual source system, or the labelled synthetic generator. |
| `source_record_id` | string | — | identifier | Stable source-record identity, masked where required. |
| `source_period_start` | date | — | point_in_time | True start of the observed or financial-statement period. Not a guessed reporting date. |
| `source_period_end` | date | — | point_in_time | True end of the observed or financial-statement period. |
| `source_published_at` | timestamp | — | point_in_time | When the source published this observation. |
| `source_available_at` | timestamp | — | point_in_time | When this observation became available to the bank. Point-in-time control: an observation is only usable at a snapshot whose cutoff is at or after this. |
| `source_version` | string | — | identifier | Source definition version behind this value. |
| `mapping_version` | string | — | identifier | Transformation/mapping version applied on ingestion. |
| `ingested_at` | timestamp | — | point_in_time | Ingestion timestamp. |
| `provenance_id` | string | — | identifier | Permission-scoped lineage reference for this row. |
| `value_origin` | string | — | enum | How the value came to be. These are never conflated: a carried-forward annual statement is not a newly observed quarterly one, and a forecast is not an actual. |
| `missing_reason` | string | — | enum | Why a value is absent. Absent is not zero, and 'not_applicable' is not 'unknown'. |
| `currency_code` | string | — | enum | Original currency of the source amount. |
| `reporting_currency` | string | — | enum | The release's reporting currency. 'RCY' in a column name means this. |
| `fx_to_reporting_currency` | float | ratio | not_additive | Conversion scalar recorded for this snapshot. A stored scalar, not access to a separate FX domain. |
| `amount_scale` | string | — | enum | Unit, thousand, million. Normalized on ingestion; the source convention is preserved here. |
| `record_status` | string | — | enum | Whether this row is available, partial or not applicable. |
| `observation_age_days` | integer | days | not_additive | Age in days of the genuine observation behind this row, at the snapshot date. |
| `facility_id` | string | — | identifier | Stable facility identifier. |
| `borrower_id` | string | — | identifier | Stable borrower identifier. |
| `position_id` | string | — | identifier | Tranche, currency or position identity, present because the source measures these independently. Part of the atomic key: ignoring it either duplicates or loses exposure. |
| `borrower_name` | string | — | identifier | Display name or stable pseudonym. |
| `borrower_group_id` | string | — | identifier | Minimal grouping key where genuinely available. Not an unrestricted group-intelligence domain. |
| `sector_code` | string | — | enum | Source industry classification code. |
| `sector_name` | string | — | enum | Source industry classification name, for portfolio filters. |
| `country_code` | string | — | enum | Borrower or exposure country, ISO 3166-1 alpha-2. Also the join key to the macro window's country_or_region. |
| `portfolio_id` | string | — | enum | Authorized portfolio filter. |
| `product_type` | string | — | enum | Lending product type. |
| `facility_status` | string | — | enum | Source lifecycle status. A closed or matured facility has no rows after its exit quarter; that is coverage, not missing data. |
| `origination_date` | date | — | point_in_time | Contractual origination date. |
| `maturity_date` | date | — | point_in_time | Contractual maturity date. |
| `remaining_maturity_months` | float | months | not_additive | Remaining contractual horizon in months at the snapshot date. |
| `approved_limit` | float | RCY | additive | Source facility limit for this exposure. Not a separate portfolio-limits service. |
| `drawn_balance` | float | RCY | additive | Source outstanding drawn amount. |
| `undrawn_balance` | float | RCY | additive | Available committed undrawn amount under the source definition. |
| `gross_carrying_amount` | float | RCY | additive | Source gross accounting carrying amount. |
| `accrued_interest` | float | RCY | additive | Accrued interest. Whether it is inside gross_carrying_amount is declared by accrued_interest_in_balance. |
| `accrued_interest_in_balance` | boolean | — | enum | True when accrued_interest is already included in gross_carrying_amount, so it is not added twice. |
| `ead_reported` | float | RCY | additive | Reported exposure at default on the source horizon and basis. |
| `ead_pit` | float | RCY | additive | Source point-in-time EAD. Its horizon is ead_definition_id's. |
| `ead_ttc` | float | RCY | additive | Source through-the-cycle EAD where the source defines one; otherwise missing. Not a substitute for ead_pit. |
| `ccf_pit` | float | fraction_0_1 | not_additive | Point-in-time credit conversion factor, where actually provided. |
| `ccf_ttc` | float | fraction_0_1 | not_additive | Through-the-cycle credit conversion factor, where actually provided. No conversion is invented when it is absent. |
| `pd_pit_12m` | float | probability_0_1 | not_additive | Point-in-time probability of default over the next 12 months. |
| `pd_pit_lifetime` | float | probability_0_1 | not_additive | Point-in-time CUMULATIVE PD over the source-defined remaining lifetime. A different concept from pd_pit_12m, not a longer version of it. |
| `pd_ttc_12m` | float | probability_0_1 | not_additive | Through-the-cycle 12-month PD on the documented horizon. |
| `pd_ttc_lifetime` | float | probability_0_1 | not_additive | Through-the-cycle lifetime cumulative PD, supplied only where the source defines one. Never a relabelled annual PD. |
| `pd_pit_12m_at_origination` | float | probability_0_1 | not_additive | PIT 12-month PD at initial recognition, the SICR comparison baseline. |
| `pd_pit_lifetime_at_origination` | float | probability_0_1 | not_additive | PIT lifetime PD at initial recognition. |
| `pd_ttc_12m_at_origination` | float | probability_0_1 | not_additive | TTC 12-month PD at initial recognition, where supplied. |
| `pd_ttc_lifetime_at_origination` | float | probability_0_1 | not_additive | TTC lifetime PD at initial recognition, where supplied. |
| `pd_lifetime_horizon_months` | float | months | not_additive | The actual remaining horizon underlying the lifetime PD. Not automatically twenty quarters and not the reporting calendar. |
| `pd_definition_id` | string | — | identifier | Which PD definition and basis these values follow. |
| `pd_parameter_version` | string | — | identifier | Source parameter version for the PD values. |
| `lgd_pit` | float | fraction_0_1 | not_additive | Point-in-time loss given default on the source calibration. |
| `lgd_ttc` | float | fraction_0_1 | not_additive | Through-the-cycle loss given default. A distinct source field, not a fallback for lgd_pit. |
| `lgd_downturn` | float | fraction_0_1 | not_additive | Source downturn LGD where recorded. Cockpit does not generate it as a stress scenario. |
| `lgd_definition_id` | string | — | identifier | Definition and timing assumptions behind the LGD values. |
| `ead_definition_id` | string | — | identifier | Definition, horizon and timing assumptions behind the EAD values. |
| `ifrs9_stage` | integer | — | enum | Stored IFRS 9 stage. Cockpit reads it; it does not assign a new one. |
| `stage_reason_recorded` | string | — | not_additive | Recorded explanation for the stage, where supplied. Its absence is not an invitation to invent causation. |
| `sicr_flag` | boolean | — | enum | Stored significant-increase-in-credit-risk determination. |
| `sicr_reason_recorded` | string | — | not_additive | Source reason recorded for the SICR determination. |
| `default_flag` | boolean | — | enum | Default status. Separate from the AAA-to-C rating scale: grade C is not mechanically default. |
| `default_date` | date | — | point_in_time | Observed date of default. |
| `days_past_due` | integer | days | not_additive | Stored contractual delinquency in days. |
| `effective_interest_rate` | float | fraction_per_annum | not_additive | Source effective interest rate, per annum as a fraction. |
| `ecl_12m_reported` | float | RCY | additive | Reported 12-month ECL. A different horizon from lifetime ECL, never compared with it as though they were the same measure. |
| `ecl_lifetime_reported` | float | RCY | additive | Reported lifetime ECL. |
| `ecl_reported` | float | RCY | additive | The booked ECL for this position and source run. For a stage 1 position this is the 12-month measure; for stage 2 and 3, lifetime. |
| `ecl_modelled` | float | RCY | additive | The model component of the booked ECL, where separately supplied. |
| `ecl_overlay` | float | RCY | additive | The overlay component, where separately supplied. Adding it to ecl_modelled reproduces ecl_reported only when both are present. |
| `ecl_coverage_ratio` | float | fraction | not_additive | ECL over the named balance denominator. The denominator is ecl_coverage_denominator, not assumed. |
| `ecl_coverage_denominator` | string | — | enum | Which balance ecl_coverage_ratio divides by. |
| `ifrs9_run_id` | string | — | identifier | Stored accounting run identity. |
| `ifrs9_model_version` | string | — | identifier | Stored model version behind the run. |
| `ifrs9_input_coverage_status` | string | — | enum | Whether the stored ECL can be reconstructed from the inputs present, only approximated, or only compared. States the factual position rather than forcing a PD x LGD x EAD reconstruction. |
| `cash_deposits_asset_count` | integer | count | not_additive | Cash deposits: Number of DISTINCT assets of this type linked to the position. An asset securing three facilities counts once in each, so summing this across facilities counts it three times. |
| `cash_deposits_gross_value_rcy` | float | RCY | not_additive | Cash deposits: Unadjusted source value of the WHOLE assets of this type linked here, in reporting currency. Summing this across facilities double counts a shared asset; use the allocated figure instead. |
| `cash_deposits_allocated_gross_value_rcy` | float | RCY | additive | Cash deposits: The gross value actually attributable to THIS position under the source allocation share. This is the figure that adds up across facilities. |
| `cash_deposits_haircut_weighted` | float | fraction_0_1 | not_additive | Cash deposits: Weighted average total haircut for this type. The weighting base is named by collateral_haircut_weighting_base, and assets with a missing haircut are EXCLUDED and reported rather than treated as zero. |
| `cash_deposits_haircut_amount_rcy` | float | RCY | not_additive | Cash deposits: The value deducted by haircuts on the declared base. Applied once; a net value never receives a haircut twice. |
| `cash_deposits_net_value_rcy` | float | RCY | not_additive | Cash deposits: Source net realizable value of the whole assets of this type after the stated adjustments. |
| `cash_deposits_allocated_net_value_rcy` | float | RCY | additive | Cash deposits: The net value attributable to THIS position. The additive collateral measure. |
| `cash_deposits_valuation_missing_rate` | float | fraction_0_1 | not_additive | Cash deposits: Fraction of assets of this type here with no usable valuation. An asset present with an unknown valuation is NOT the same as no collateral of that type. |
| `cash_deposits_overdue_valuation_count` | integer | count | not_additive | Cash deposits: Assets of this type whose valuation is past its policy expiry. |
| `government_securities_asset_count` | integer | count | not_additive | Government securities: Number of DISTINCT assets of this type linked to the position. An asset securing three facilities counts once in each, so summing this across facilities counts it three times. |
| `government_securities_gross_value_rcy` | float | RCY | not_additive | Government securities: Unadjusted source value of the WHOLE assets of this type linked here, in reporting currency. Summing this across facilities double counts a shared asset; use the allocated figure instead. |
| `government_securities_allocated_gross_value_rcy` | float | RCY | additive | Government securities: The gross value actually attributable to THIS position under the source allocation share. This is the figure that adds up across facilities. |
| `government_securities_haircut_weighted` | float | fraction_0_1 | not_additive | Government securities: Weighted average total haircut for this type. The weighting base is named by collateral_haircut_weighting_base, and assets with a missing haircut are EXCLUDED and reported rather than treated as zero. |
| `government_securities_haircut_amount_rcy` | float | RCY | not_additive | Government securities: The value deducted by haircuts on the declared base. Applied once; a net value never receives a haircut twice. |
| `government_securities_net_value_rcy` | float | RCY | not_additive | Government securities: Source net realizable value of the whole assets of this type after the stated adjustments. |
| `government_securities_allocated_net_value_rcy` | float | RCY | additive | Government securities: The net value attributable to THIS position. The additive collateral measure. |
| `government_securities_valuation_missing_rate` | float | fraction_0_1 | not_additive | Government securities: Fraction of assets of this type here with no usable valuation. An asset present with an unknown valuation is NOT the same as no collateral of that type. |
| `government_securities_overdue_valuation_count` | integer | count | not_additive | Government securities: Assets of this type whose valuation is past its policy expiry. |
| `bank_guarantees_asset_count` | integer | count | not_additive | Bank guarantees: Number of DISTINCT assets of this type linked to the position. An asset securing three facilities counts once in each, so summing this across facilities counts it three times. |
| `bank_guarantees_gross_value_rcy` | float | RCY | not_additive | Bank guarantees: Unadjusted source value of the WHOLE assets of this type linked here, in reporting currency. Summing this across facilities double counts a shared asset; use the allocated figure instead. |
| `bank_guarantees_allocated_gross_value_rcy` | float | RCY | additive | Bank guarantees: The gross value actually attributable to THIS position under the source allocation share. This is the figure that adds up across facilities. |
| `bank_guarantees_haircut_weighted` | float | fraction_0_1 | not_additive | Bank guarantees: Weighted average total haircut for this type. The weighting base is named by collateral_haircut_weighting_base, and assets with a missing haircut are EXCLUDED and reported rather than treated as zero. |
| `bank_guarantees_haircut_amount_rcy` | float | RCY | not_additive | Bank guarantees: The value deducted by haircuts on the declared base. Applied once; a net value never receives a haircut twice. |
| `bank_guarantees_net_value_rcy` | float | RCY | not_additive | Bank guarantees: Source net realizable value of the whole assets of this type after the stated adjustments. |
| `bank_guarantees_allocated_net_value_rcy` | float | RCY | additive | Bank guarantees: The net value attributable to THIS position. The additive collateral measure. |
| `bank_guarantees_valuation_missing_rate` | float | fraction_0_1 | not_additive | Bank guarantees: Fraction of assets of this type here with no usable valuation. An asset present with an unknown valuation is NOT the same as no collateral of that type. |
| `bank_guarantees_overdue_valuation_count` | integer | count | not_additive | Bank guarantees: Assets of this type whose valuation is past its policy expiry. |
| `residential_property_asset_count` | integer | count | not_additive | Residential property: Number of DISTINCT assets of this type linked to the position. An asset securing three facilities counts once in each, so summing this across facilities counts it three times. |
| `residential_property_gross_value_rcy` | float | RCY | not_additive | Residential property: Unadjusted source value of the WHOLE assets of this type linked here, in reporting currency. Summing this across facilities double counts a shared asset; use the allocated figure instead. |
| `residential_property_allocated_gross_value_rcy` | float | RCY | additive | Residential property: The gross value actually attributable to THIS position under the source allocation share. This is the figure that adds up across facilities. |
| `residential_property_haircut_weighted` | float | fraction_0_1 | not_additive | Residential property: Weighted average total haircut for this type. The weighting base is named by collateral_haircut_weighting_base, and assets with a missing haircut are EXCLUDED and reported rather than treated as zero. |
| `residential_property_haircut_amount_rcy` | float | RCY | not_additive | Residential property: The value deducted by haircuts on the declared base. Applied once; a net value never receives a haircut twice. |
| `residential_property_net_value_rcy` | float | RCY | not_additive | Residential property: Source net realizable value of the whole assets of this type after the stated adjustments. |
| `residential_property_allocated_net_value_rcy` | float | RCY | additive | Residential property: The net value attributable to THIS position. The additive collateral measure. |
| `residential_property_valuation_missing_rate` | float | fraction_0_1 | not_additive | Residential property: Fraction of assets of this type here with no usable valuation. An asset present with an unknown valuation is NOT the same as no collateral of that type. |
| `residential_property_overdue_valuation_count` | integer | count | not_additive | Residential property: Assets of this type whose valuation is past its policy expiry. |
| `commercial_property_asset_count` | integer | count | not_additive | Commercial property: Number of DISTINCT assets of this type linked to the position. An asset securing three facilities counts once in each, so summing this across facilities counts it three times. |
| `commercial_property_gross_value_rcy` | float | RCY | not_additive | Commercial property: Unadjusted source value of the WHOLE assets of this type linked here, in reporting currency. Summing this across facilities double counts a shared asset; use the allocated figure instead. |
| `commercial_property_allocated_gross_value_rcy` | float | RCY | additive | Commercial property: The gross value actually attributable to THIS position under the source allocation share. This is the figure that adds up across facilities. |
| `commercial_property_haircut_weighted` | float | fraction_0_1 | not_additive | Commercial property: Weighted average total haircut for this type. The weighting base is named by collateral_haircut_weighting_base, and assets with a missing haircut are EXCLUDED and reported rather than treated as zero. |
| `commercial_property_haircut_amount_rcy` | float | RCY | not_additive | Commercial property: The value deducted by haircuts on the declared base. Applied once; a net value never receives a haircut twice. |
| `commercial_property_net_value_rcy` | float | RCY | not_additive | Commercial property: Source net realizable value of the whole assets of this type after the stated adjustments. |
| `commercial_property_allocated_net_value_rcy` | float | RCY | additive | Commercial property: The net value attributable to THIS position. The additive collateral measure. |
| `commercial_property_valuation_missing_rate` | float | fraction_0_1 | not_additive | Commercial property: Fraction of assets of this type here with no usable valuation. An asset present with an unknown valuation is NOT the same as no collateral of that type. |
| `commercial_property_overdue_valuation_count` | integer | count | not_additive | Commercial property: Assets of this type whose valuation is past its policy expiry. |
| `plant_machinery_asset_count` | integer | count | not_additive | Plant machinery: Number of DISTINCT assets of this type linked to the position. An asset securing three facilities counts once in each, so summing this across facilities counts it three times. |
| `plant_machinery_gross_value_rcy` | float | RCY | not_additive | Plant machinery: Unadjusted source value of the WHOLE assets of this type linked here, in reporting currency. Summing this across facilities double counts a shared asset; use the allocated figure instead. |
| `plant_machinery_allocated_gross_value_rcy` | float | RCY | additive | Plant machinery: The gross value actually attributable to THIS position under the source allocation share. This is the figure that adds up across facilities. |
| `plant_machinery_haircut_weighted` | float | fraction_0_1 | not_additive | Plant machinery: Weighted average total haircut for this type. The weighting base is named by collateral_haircut_weighting_base, and assets with a missing haircut are EXCLUDED and reported rather than treated as zero. |
| `plant_machinery_haircut_amount_rcy` | float | RCY | not_additive | Plant machinery: The value deducted by haircuts on the declared base. Applied once; a net value never receives a haircut twice. |
| `plant_machinery_net_value_rcy` | float | RCY | not_additive | Plant machinery: Source net realizable value of the whole assets of this type after the stated adjustments. |
| `plant_machinery_allocated_net_value_rcy` | float | RCY | additive | Plant machinery: The net value attributable to THIS position. The additive collateral measure. |
| `plant_machinery_valuation_missing_rate` | float | fraction_0_1 | not_additive | Plant machinery: Fraction of assets of this type here with no usable valuation. An asset present with an unknown valuation is NOT the same as no collateral of that type. |
| `plant_machinery_overdue_valuation_count` | integer | count | not_additive | Plant machinery: Assets of this type whose valuation is past its policy expiry. |
| `vehicles_asset_count` | integer | count | not_additive | Vehicles: Number of DISTINCT assets of this type linked to the position. An asset securing three facilities counts once in each, so summing this across facilities counts it three times. |
| `vehicles_gross_value_rcy` | float | RCY | not_additive | Vehicles: Unadjusted source value of the WHOLE assets of this type linked here, in reporting currency. Summing this across facilities double counts a shared asset; use the allocated figure instead. |
| `vehicles_allocated_gross_value_rcy` | float | RCY | additive | Vehicles: The gross value actually attributable to THIS position under the source allocation share. This is the figure that adds up across facilities. |
| `vehicles_haircut_weighted` | float | fraction_0_1 | not_additive | Vehicles: Weighted average total haircut for this type. The weighting base is named by collateral_haircut_weighting_base, and assets with a missing haircut are EXCLUDED and reported rather than treated as zero. |
| `vehicles_haircut_amount_rcy` | float | RCY | not_additive | Vehicles: The value deducted by haircuts on the declared base. Applied once; a net value never receives a haircut twice. |
| `vehicles_net_value_rcy` | float | RCY | not_additive | Vehicles: Source net realizable value of the whole assets of this type after the stated adjustments. |
| `vehicles_allocated_net_value_rcy` | float | RCY | additive | Vehicles: The net value attributable to THIS position. The additive collateral measure. |
| `vehicles_valuation_missing_rate` | float | fraction_0_1 | not_additive | Vehicles: Fraction of assets of this type here with no usable valuation. An asset present with an unknown valuation is NOT the same as no collateral of that type. |
| `vehicles_overdue_valuation_count` | integer | count | not_additive | Vehicles: Assets of this type whose valuation is past its policy expiry. |
| `inventory_asset_count` | integer | count | not_additive | Inventory: Number of DISTINCT assets of this type linked to the position. An asset securing three facilities counts once in each, so summing this across facilities counts it three times. |
| `inventory_gross_value_rcy` | float | RCY | not_additive | Inventory: Unadjusted source value of the WHOLE assets of this type linked here, in reporting currency. Summing this across facilities double counts a shared asset; use the allocated figure instead. |
| `inventory_allocated_gross_value_rcy` | float | RCY | additive | Inventory: The gross value actually attributable to THIS position under the source allocation share. This is the figure that adds up across facilities. |
| `inventory_haircut_weighted` | float | fraction_0_1 | not_additive | Inventory: Weighted average total haircut for this type. The weighting base is named by collateral_haircut_weighting_base, and assets with a missing haircut are EXCLUDED and reported rather than treated as zero. |
| `inventory_haircut_amount_rcy` | float | RCY | not_additive | Inventory: The value deducted by haircuts on the declared base. Applied once; a net value never receives a haircut twice. |
| `inventory_net_value_rcy` | float | RCY | not_additive | Inventory: Source net realizable value of the whole assets of this type after the stated adjustments. |
| `inventory_allocated_net_value_rcy` | float | RCY | additive | Inventory: The net value attributable to THIS position. The additive collateral measure. |
| `inventory_valuation_missing_rate` | float | fraction_0_1 | not_additive | Inventory: Fraction of assets of this type here with no usable valuation. An asset present with an unknown valuation is NOT the same as no collateral of that type. |
| `inventory_overdue_valuation_count` | integer | count | not_additive | Inventory: Assets of this type whose valuation is past its policy expiry. |
| `receivables_asset_count` | integer | count | not_additive | Receivables: Number of DISTINCT assets of this type linked to the position. An asset securing three facilities counts once in each, so summing this across facilities counts it three times. |
| `receivables_gross_value_rcy` | float | RCY | not_additive | Receivables: Unadjusted source value of the WHOLE assets of this type linked here, in reporting currency. Summing this across facilities double counts a shared asset; use the allocated figure instead. |
| `receivables_allocated_gross_value_rcy` | float | RCY | additive | Receivables: The gross value actually attributable to THIS position under the source allocation share. This is the figure that adds up across facilities. |
| `receivables_haircut_weighted` | float | fraction_0_1 | not_additive | Receivables: Weighted average total haircut for this type. The weighting base is named by collateral_haircut_weighting_base, and assets with a missing haircut are EXCLUDED and reported rather than treated as zero. |
| `receivables_haircut_amount_rcy` | float | RCY | not_additive | Receivables: The value deducted by haircuts on the declared base. Applied once; a net value never receives a haircut twice. |
| `receivables_net_value_rcy` | float | RCY | not_additive | Receivables: Source net realizable value of the whole assets of this type after the stated adjustments. |
| `receivables_allocated_net_value_rcy` | float | RCY | additive | Receivables: The net value attributable to THIS position. The additive collateral measure. |
| `receivables_valuation_missing_rate` | float | fraction_0_1 | not_additive | Receivables: Fraction of assets of this type here with no usable valuation. An asset present with an unknown valuation is NOT the same as no collateral of that type. |
| `receivables_overdue_valuation_count` | integer | count | not_additive | Receivables: Assets of this type whose valuation is past its policy expiry. |
| `listed_equities_asset_count` | integer | count | not_additive | Listed equities: Number of DISTINCT assets of this type linked to the position. An asset securing three facilities counts once in each, so summing this across facilities counts it three times. |
| `listed_equities_gross_value_rcy` | float | RCY | not_additive | Listed equities: Unadjusted source value of the WHOLE assets of this type linked here, in reporting currency. Summing this across facilities double counts a shared asset; use the allocated figure instead. |
| `listed_equities_allocated_gross_value_rcy` | float | RCY | additive | Listed equities: The gross value actually attributable to THIS position under the source allocation share. This is the figure that adds up across facilities. |
| `listed_equities_haircut_weighted` | float | fraction_0_1 | not_additive | Listed equities: Weighted average total haircut for this type. The weighting base is named by collateral_haircut_weighting_base, and assets with a missing haircut are EXCLUDED and reported rather than treated as zero. |
| `listed_equities_haircut_amount_rcy` | float | RCY | not_additive | Listed equities: The value deducted by haircuts on the declared base. Applied once; a net value never receives a haircut twice. |
| `listed_equities_net_value_rcy` | float | RCY | not_additive | Listed equities: Source net realizable value of the whole assets of this type after the stated adjustments. |
| `listed_equities_allocated_net_value_rcy` | float | RCY | additive | Listed equities: The net value attributable to THIS position. The additive collateral measure. |
| `listed_equities_valuation_missing_rate` | float | fraction_0_1 | not_additive | Listed equities: Fraction of assets of this type here with no usable valuation. An asset present with an unknown valuation is NOT the same as no collateral of that type. |
| `listed_equities_overdue_valuation_count` | integer | count | not_additive | Listed equities: Assets of this type whose valuation is past its policy expiry. |
| `debt_securities_asset_count` | integer | count | not_additive | Debt securities: Number of DISTINCT assets of this type linked to the position. An asset securing three facilities counts once in each, so summing this across facilities counts it three times. |
| `debt_securities_gross_value_rcy` | float | RCY | not_additive | Debt securities: Unadjusted source value of the WHOLE assets of this type linked here, in reporting currency. Summing this across facilities double counts a shared asset; use the allocated figure instead. |
| `debt_securities_allocated_gross_value_rcy` | float | RCY | additive | Debt securities: The gross value actually attributable to THIS position under the source allocation share. This is the figure that adds up across facilities. |
| `debt_securities_haircut_weighted` | float | fraction_0_1 | not_additive | Debt securities: Weighted average total haircut for this type. The weighting base is named by collateral_haircut_weighting_base, and assets with a missing haircut are EXCLUDED and reported rather than treated as zero. |
| `debt_securities_haircut_amount_rcy` | float | RCY | not_additive | Debt securities: The value deducted by haircuts on the declared base. Applied once; a net value never receives a haircut twice. |
| `debt_securities_net_value_rcy` | float | RCY | not_additive | Debt securities: Source net realizable value of the whole assets of this type after the stated adjustments. |
| `debt_securities_allocated_net_value_rcy` | float | RCY | additive | Debt securities: The net value attributable to THIS position. The additive collateral measure. |
| `debt_securities_valuation_missing_rate` | float | fraction_0_1 | not_additive | Debt securities: Fraction of assets of this type here with no usable valuation. An asset present with an unknown valuation is NOT the same as no collateral of that type. |
| `debt_securities_overdue_valuation_count` | integer | count | not_additive | Debt securities: Assets of this type whose valuation is past its policy expiry. |
| `other_collateral_asset_count` | integer | count | not_additive | Other collateral: Number of DISTINCT assets of this type linked to the position. An asset securing three facilities counts once in each, so summing this across facilities counts it three times. |
| `other_collateral_gross_value_rcy` | float | RCY | not_additive | Other collateral: Unadjusted source value of the WHOLE assets of this type linked here, in reporting currency. Summing this across facilities double counts a shared asset; use the allocated figure instead. |
| `other_collateral_allocated_gross_value_rcy` | float | RCY | additive | Other collateral: The gross value actually attributable to THIS position under the source allocation share. This is the figure that adds up across facilities. |
| `other_collateral_haircut_weighted` | float | fraction_0_1 | not_additive | Other collateral: Weighted average total haircut for this type. The weighting base is named by collateral_haircut_weighting_base, and assets with a missing haircut are EXCLUDED and reported rather than treated as zero. |
| `other_collateral_haircut_amount_rcy` | float | RCY | not_additive | Other collateral: The value deducted by haircuts on the declared base. Applied once; a net value never receives a haircut twice. |
| `other_collateral_net_value_rcy` | float | RCY | not_additive | Other collateral: Source net realizable value of the whole assets of this type after the stated adjustments. |
| `other_collateral_allocated_net_value_rcy` | float | RCY | additive | Other collateral: The net value attributable to THIS position. The additive collateral measure. |
| `other_collateral_valuation_missing_rate` | float | fraction_0_1 | not_additive | Other collateral: Fraction of assets of this type here with no usable valuation. An asset present with an unknown valuation is NOT the same as no collateral of that type. |
| `other_collateral_overdue_valuation_count` | integer | count | not_additive | Other collateral: Assets of this type whose valuation is past its policy expiry. |
| `collateral_total_gross_value_rcy` | float | RCY | not_additive | Gross value of all whole assets linked to this position, all types. NOT additive across facilities: a shared asset appears in each. |
| `collateral_total_allocated_gross_value_rcy` | float | RCY | additive | Gross value attributable to this position across all types. Additive. |
| `collateral_total_allocated_net_value_rcy` | float | RCY | additive | Net value attributable to this position across all types, after haircuts. Additive, and the figure a coverage ratio should use. |
| `collateral_haircut_weighting_base` | string | — | enum | What the weighted haircuts are weighted by. |
| `collateral_coverage_ratio` | float | times | not_additive | Allocated net collateral value / the balance named in collateral_coverage_denominator. |
| `collateral_coverage_denominator` | string | — | enum | Which balance the collateral coverage ratio divides by. |
| `allocation_coverage_status` | string | — | enum | Whether allocations are known for every linked asset, some value is shared and unallocated, or allocation data is unavailable. |

### `cockpit_ifrs9_detail` — Stored IFRS 9 scenario and term detail

**Grain:** facility position x reporting_quarter x ifrs9_run_id x scenario_id x term_horizon_index -- stored IFRS 9 parameters and results only

44 columns.

| Column | Type | Unit | Aggregation | Definition |
|---|---|---|---|---|
| `tenant_id` | string | — | identifier | Authenticated data owner. Enforced by the server and the query principal; never trusted from model text and never a filter Opus must remember to write. |
| `domain_id` | string | — | identifier | Constant 'corporate_cockpit' for every business artifact this runtime may reach. |
| `dataset_release_id` | string | — | identifier | Immutable release selection, pinned for the whole user request. Two releases are never mixed in one answer. |
| `reporting_quarter` | string | — | point_in_time | One of the twenty authorized anchor quarter labels, e.g. '2026Q2'. |
| `quarter_end_date` | date | — | point_in_time | Calendar end date of the reporting quarter. |
| `data_cutoff_at` | timestamp | — | point_in_time | Latest information timestamp permitted for this snapshot. Nothing published after it was knowable at it. |
| `source_system` | string | — | identifier | Actual source system, or the labelled synthetic generator. |
| `source_record_id` | string | — | identifier | Stable source-record identity, masked where required. |
| `source_period_start` | date | — | point_in_time | True start of the observed or financial-statement period. Not a guessed reporting date. |
| `source_period_end` | date | — | point_in_time | True end of the observed or financial-statement period. |
| `source_published_at` | timestamp | — | point_in_time | When the source published this observation. |
| `source_available_at` | timestamp | — | point_in_time | When this observation became available to the bank. Point-in-time control: an observation is only usable at a snapshot whose cutoff is at or after this. |
| `source_version` | string | — | identifier | Source definition version behind this value. |
| `mapping_version` | string | — | identifier | Transformation/mapping version applied on ingestion. |
| `ingested_at` | timestamp | — | point_in_time | Ingestion timestamp. |
| `provenance_id` | string | — | identifier | Permission-scoped lineage reference for this row. |
| `value_origin` | string | — | enum | How the value came to be. These are never conflated: a carried-forward annual statement is not a newly observed quarterly one, and a forecast is not an actual. |
| `missing_reason` | string | — | enum | Why a value is absent. Absent is not zero, and 'not_applicable' is not 'unknown'. |
| `currency_code` | string | — | enum | Original currency of the source amount. |
| `reporting_currency` | string | — | enum | The release's reporting currency. 'RCY' in a column name means this. |
| `fx_to_reporting_currency` | float | ratio | not_additive | Conversion scalar recorded for this snapshot. A stored scalar, not access to a separate FX domain. |
| `amount_scale` | string | — | enum | Unit, thousand, million. Normalized on ingestion; the source convention is preserved here. |
| `record_status` | string | — | enum | Whether this row is available, partial or not applicable. |
| `observation_age_days` | integer | days | not_additive | Age in days of the genuine observation behind this row, at the snapshot date. |
| `facility_id` | string | — | identifier | Facility identifier. |
| `position_id` | string | — | identifier | Position identifier. |
| `ifrs9_run_id` | string | — | identifier | Stored accounting run identity. |
| `scenario_id` | string | — | enum | A scenario ALREADY STORED by the source run. Not a new what-if scenario, and not something Cockpit may create. |
| `scenario_name` | string | — | enum | Source scenario label. |
| `scenario_weight` | float | probability_0_1 | not_additive | Stored probability weight. Weights across a run's scenarios sum to one; a scenario ECL is not the booked ECL. |
| `scenario_ecl` | float | RCY | additive | Stored ECL under this scenario alone. Summing across scenarios overstates the booked figure; weight them. |
| `scenario_pd_pit_12m` | float | probability_0_1 | not_additive | Stored scenario 12-month PIT PD. |
| `scenario_pd_pit_lifetime` | float | probability_0_1 | not_additive | Stored scenario lifetime PIT PD. |
| `scenario_lgd` | float | fraction_0_1 | not_additive | Stored scenario LGD. |
| `scenario_ead` | float | RCY | additive | Stored scenario EAD. |
| `term_horizon_index` | integer | — | ordinal | Position on the stored parameter curve. A projection horizon, NOT one of the twenty reporting snapshots. |
| `term_horizon_end_date` | date | — | point_in_time | End date of that curve point. |
| `term_pd_marginal` | float | probability_0_1 | not_additive | Marginal (conditional) default probability in this horizon, given survival to its start. |
| `term_pd_cumulative` | float | probability_0_1 | not_additive | Cumulative (unconditional) default probability to the end of this horizon. Not the sum of marginals. |
| `term_survival` | float | probability_0_1 | not_additive | Probability of surviving to the start of this horizon. |
| `term_lgd` | float | fraction_0_1 | not_additive | LGD applied at this horizon. |
| `term_ead` | float | RCY | additive | EAD projected at this horizon. |
| `term_discount_factor` | float | ratio | not_additive | Discount factor applied at this horizon, on the effective interest rate. |
| `term_expected_shortfall` | float | RCY | additive | Discounted expected loss contributed by this horizon. Summing across horizons reproduces the lifetime measure for this scenario. |

### `cockpit_borrower_financial_quarter` — Borrower financial statements

**Grain:** borrower_id x reporting_quarter x statement_scope -- BORROWER grain. One borrower may secure several facilities; joining this to cockpit_facility_quarter repeats the statement once per facility

135 columns.

| Column | Type | Unit | Aggregation | Definition |
|---|---|---|---|---|
| `tenant_id` | string | — | identifier | Authenticated data owner. Enforced by the server and the query principal; never trusted from model text and never a filter Opus must remember to write. |
| `domain_id` | string | — | identifier | Constant 'corporate_cockpit' for every business artifact this runtime may reach. |
| `dataset_release_id` | string | — | identifier | Immutable release selection, pinned for the whole user request. Two releases are never mixed in one answer. |
| `reporting_quarter` | string | — | point_in_time | One of the twenty authorized anchor quarter labels, e.g. '2026Q2'. |
| `quarter_end_date` | date | — | point_in_time | Calendar end date of the reporting quarter. |
| `data_cutoff_at` | timestamp | — | point_in_time | Latest information timestamp permitted for this snapshot. Nothing published after it was knowable at it. |
| `source_system` | string | — | identifier | Actual source system, or the labelled synthetic generator. |
| `source_record_id` | string | — | identifier | Stable source-record identity, masked where required. |
| `source_period_start` | date | — | point_in_time | True start of the observed or financial-statement period. Not a guessed reporting date. |
| `source_period_end` | date | — | point_in_time | True end of the observed or financial-statement period. |
| `source_published_at` | timestamp | — | point_in_time | When the source published this observation. |
| `source_available_at` | timestamp | — | point_in_time | When this observation became available to the bank. Point-in-time control: an observation is only usable at a snapshot whose cutoff is at or after this. |
| `source_version` | string | — | identifier | Source definition version behind this value. |
| `mapping_version` | string | — | identifier | Transformation/mapping version applied on ingestion. |
| `ingested_at` | timestamp | — | point_in_time | Ingestion timestamp. |
| `provenance_id` | string | — | identifier | Permission-scoped lineage reference for this row. |
| `value_origin` | string | — | enum | How the value came to be. These are never conflated: a carried-forward annual statement is not a newly observed quarterly one, and a forecast is not an actual. |
| `missing_reason` | string | — | enum | Why a value is absent. Absent is not zero, and 'not_applicable' is not 'unknown'. |
| `currency_code` | string | — | enum | Original currency of the source amount. |
| `reporting_currency` | string | — | enum | The release's reporting currency. 'RCY' in a column name means this. |
| `fx_to_reporting_currency` | float | ratio | not_additive | Conversion scalar recorded for this snapshot. A stored scalar, not access to a separate FX domain. |
| `amount_scale` | string | — | enum | Unit, thousand, million. Normalized on ingestion; the source convention is preserved here. |
| `record_status` | string | — | enum | Whether this row is available, partial or not applicable. |
| `observation_age_days` | integer | days | not_additive | Age in days of the genuine observation behind this row, at the snapshot date. |
| `borrower_id` | string | — | identifier | Stable borrower identifier. Part of this relation's grain: one borrower has ONE statement per quarter and scope, however many facilities it holds. |
| `statement_scope` | string | — | enum | Standalone or consolidated, and the comparison basis chosen. Never silently mixed across quarters. |
| `statement_id` | string | — | identifier | Actual financial report identity. |
| `statement_version` | string | — | identifier | Report vintage, e.g. a restatement. |
| `statement_period_basis` | string | — | enum | Quarter-only, year-to-date, annual or trailing twelve months. A ratio built from two different bases is not comparable, and this field is how that is detected. |
| `statement_period_days` | integer | days | not_additive | Actual length of the statement period in days. Day-based ratios use this, not an assumed 90 or 365. |
| `audited_flag` | boolean | — | enum | Observed audit status. Not a model assessment. |
| `audit_opinion` | string | — | enum | Recorded audit opinion where present. |
| `cash_and_cash_equivalents` | float | RCY | not_additive | Reported cash balance. |
| `restricted_cash` | float | RCY | not_additive | Cash unavailable for ordinary debt service. |
| `short_term_investments` | float | RCY | not_additive | Current financial investments. |
| `trade_receivables_gross` | float | RCY | not_additive | Gross trade receivables. |
| `receivables_loss_allowance` | float | RCY | not_additive | Allowance against trade receivables, positive as an allowance. |
| `trade_receivables_net` | float | RCY | not_additive | Net trade receivables on the source definition. |
| `inventory` | float | RCY | not_additive | Reported inventories. |
| `prepayments` | float | RCY | not_additive | Current prepayments. |
| `other_current_assets` | float | RCY | not_additive | Other current assets. |
| `current_assets` | float | RCY | not_additive | Total current assets. |
| `ppe_gross` | float | RCY | not_additive | Gross property, plant and equipment. |
| `accumulated_depreciation` | float | RCY | not_additive | Accumulated depreciation, positive as a deduction. |
| `ppe_net` | float | RCY | not_additive | Net property, plant and equipment. |
| `goodwill` | float | RCY | not_additive | Reported goodwill. |
| `other_intangible_assets` | float | RCY | not_additive | Intangibles excluding goodwill. |
| `long_term_investments` | float | RCY | not_additive | Non-current investments. |
| `other_noncurrent_assets` | float | RCY | not_additive | Other non-current assets. |
| `noncurrent_assets` | float | RCY | not_additive | Total non-current assets. |
| `total_assets` | float | RCY | not_additive | Total assets. |
| `trade_payables` | float | RCY | not_additive | Trade creditors. |
| `short_term_borrowings` | float | RCY | not_additive | Short-term interest-bearing debt. |
| `current_portion_long_term_debt` | float | RCY | not_additive | Current maturities of long-term borrowings. |
| `interest_payable` | float | RCY | not_additive | Accrued interest liabilities. |
| `tax_payable` | float | RCY | not_additive | Current tax liabilities. |
| `accrued_expenses` | float | RCY | not_additive | Accrued operating expenses. |
| `other_current_liabilities` | float | RCY | not_additive | Other current liabilities. |
| `current_liabilities` | float | RCY | not_additive | Total current liabilities. |
| `long_term_debt` | float | RCY | not_additive | Non-current borrowing balance. |
| `lease_liabilities_current` | float | RCY | not_additive | Current lease liabilities. |
| `lease_liabilities_noncurrent` | float | RCY | not_additive | Non-current lease liabilities. |
| `deferred_tax_liabilities` | float | RCY | not_additive | Non-current deferred tax liabilities. |
| `provisions_noncurrent` | float | RCY | not_additive | Non-current provisions. |
| `other_noncurrent_liabilities` | float | RCY | not_additive | Other non-current liabilities. |
| `noncurrent_liabilities` | float | RCY | not_additive | Total non-current liabilities. |
| `total_liabilities` | float | RCY | not_additive | Total liabilities. |
| `share_capital` | float | RCY | not_additive | Issued share capital. |
| `retained_earnings` | float | RCY | not_additive | Accumulated retained earnings. |
| `reserves` | float | RCY | not_additive | Other equity reserves. |
| `noncontrolling_interests` | float | RCY | not_additive | Minority equity interests. |
| `shareholders_equity` | float | RCY | not_additive | Equity on a consistently applied scope. Whether it includes non-controlling interests is fixed by equity_scope. |
| `equity_scope` | string | — | enum | Whether shareholders_equity includes non-controlling interests. |
| `tangible_net_worth` | float | RCY | not_additive | Equity less goodwill and other intangibles, on the adjustments recorded in tangible_net_worth_basis. |
| `tangible_net_worth_basis` | string | — | identifier | Which items were deducted to reach tangible net worth. |
| `working_capital` | float | RCY | not_additive | Current assets less current liabilities, unless the source defines it otherwise and says so. |
| `liquid_assets` | float | RCY | not_additive | Source-defined liquid assets. The asset classes included and any restrictions are declared in liquid_assets_basis. |
| `liquid_assets_basis` | string | — | identifier | Which asset classes liquid_assets includes. |
| `total_debt` | float | RCY | not_additive | Source-defined interest-bearing debt. Whether leases are inside it is fixed by debt_lease_treatment. |
| `debt_lease_treatment` | string | — | enum | Whether lease liabilities are included in total_debt. |
| `net_debt` | float | RCY | not_additive | Total debt less the explicitly eligible cash balance, which excludes restricted cash. |
| `capital_employed` | float | RCY | not_additive | Source-defined capital employed used for the return-on-capital ratio. |
| `revenue` | float | RCY | not_additive | Net reported revenue for the exact statement period. |
| `domestic_revenue` | float | RCY | not_additive | Domestic revenue split, where supplied. |
| `export_revenue` | float | RCY | not_additive | Export revenue split, where supplied. |
| `credit_sales` | float | RCY | not_additive | Sales made on credit. Required by the strict receivables-turnover definition; where absent, that ratio is unavailable unless an alternate basis is declared. |
| `sales_returns` | float | RCY | not_additive | Returns deducted to reach net sales. |
| `sales_discounts` | float | RCY | not_additive | Discounts deducted to reach net sales. |
| `cost_of_goods_sold` | float | RCY | not_additive | Cost of sales, on a positive-expense convention. |
| `gross_profit` | float | RCY | not_additive | Reported or derived gross profit. |
| `staff_costs` | float | RCY | not_additive | Personnel costs. |
| `selling_distribution_expenses` | float | RCY | not_additive | Selling and distribution expense. |
| `administrative_expenses` | float | RCY | not_additive | Administration expense. |
| `research_development_expenses` | float | RCY | not_additive | Period research and development expense. |
| `lease_rent_expense` | float | RCY | not_additive | Lease and rent expense under the recorded accounting policy. |
| `depreciation_expense` | float | RCY | not_additive | Depreciation charge. |
| `amortization_expense` | float | RCY | not_additive | Amortization charge. |
| `other_operating_expenses` | float | RCY | not_additive | Other operating expenses. |
| `total_operating_expenses` | float | RCY | not_additive | Total operating expense. Its components are named by operating_expense_basis. |
| `other_operating_income` | float | RCY | not_additive | Other operating income. |
| `ebitda` | float | RCY | not_additive | Reported or transparently derived EBITDA. Its adjustment policy is named by ebitda_basis. |
| `ebit` | float | RCY | not_additive | Earnings before interest and taxes. |
| `interest_income` | float | RCY | not_additive | Reported interest income. |
| `interest_expense` | float | RCY | not_additive | GROSS interest expense. Not net finance cost: the coverage ratios divide by this. |
| `net_finance_cost` | float | RCY | not_additive | Net finance cost, where separately reported. |
| `foreign_exchange_gain_loss` | float | RCY | not_additive | Period FX gain or loss. Positive is a gain. |
| `exceptional_income` | float | RCY | not_additive | Separately identified non-recurring income. |
| `exceptional_expenses` | float | RCY | not_additive | Separately identified non-recurring expense. |
| `other_nonoperating_income` | float | RCY | not_additive | Other non-operating income. |
| `profit_before_tax` | float | RCY | not_additive | Profit before income taxes. |
| `tax_expense` | float | RCY | not_additive | Period tax charge. |
| `net_profit` | float | RCY | not_additive | Profit after tax for the stated scope. |
| `net_profit_attributable_to_owners` | float | RCY | not_additive | Owners' share, where supplied. |
| `dividends_declared` | float | RCY | not_additive | Distributions declared for the stated period. |
| `operating_expense_basis` | string | — | identifier | Which components total_operating_expenses includes. |
| `ebitda_basis` | string | — | identifier | Which adjustments the reported EBITDA applies. |
| `operating_cash_flow` | float | RCY | not_additive | Cash from operations for the same financial period. Not a bank-account transaction feed. |
| `capital_expenditure` | float | RCY | not_additive | Period investment outflow, positive as an outflow. |
| `free_cash_flow` | float | RCY | not_additive | Source-defined free cash flow. Derived as operating cash flow less capital expenditure only where that is appropriate and declared by fcf_basis. |
| `cash_available_for_debt_service` | float | RCY | not_additive | CFADS under the source DSCR definition. NOT automatically EBITDA. |
| `scheduled_principal_due` | float | RCY | not_additive | Principal due in the matched debt-service period. |
| `interest_due_for_debt_service` | float | RCY | not_additive | Interest due in that same period. |
| `debt_service_due` | float | RCY | not_additive | Matched scheduled principal plus interest, or the source-defined total. |
| `credit_purchases` | float | RCY | not_additive | Purchases made on credit, for payables turnover and days. Where absent, those ratios are unavailable unless an alternate basis is declared. |
| `opening_total_assets` | float | RCY | not_additive | True opening total assets for the statement period. An opening balance, NOT permission to query a twenty-first reporting snapshot. |
| `opening_shareholders_equity` | float | RCY | not_additive | True opening equity for the period. |
| `opening_inventory` | float | RCY | not_additive | True opening inventory. |
| `opening_trade_receivables_net` | float | RCY | not_additive | True opening net receivables. |
| `opening_trade_payables` | float | RCY | not_additive | True opening trade payables. |
| `opening_ppe_net` | float | RCY | not_additive | True opening net PPE. |
| `opening_working_capital` | float | RCY | not_additive | True opening working capital. |
| `opening_capital_employed` | float | RCY | not_additive | True opening capital employed. |
| `fcf_basis` | string | — | identifier | How free_cash_flow was defined for this statement. |
| `dscr_basis` | string | — | identifier | The source DSCR convention: what is in the numerator and what is in the matched debt service. |
| `financial_input_coverage` | string | — | not_additive | Flags for unavailable denominators, incompatible period bases, zero or negative denominators and source gaps in this statement. |

### `cockpit_rating_ratio_quarter` — Stored ratings and the forty ratios

**Grain:** borrower_id x reporting_quarter x rating_basis -- BORROWER grain, same repetition warning as the financial statements

170 columns.

| Column | Type | Unit | Aggregation | Definition |
|---|---|---|---|---|
| `tenant_id` | string | — | identifier | Authenticated data owner. Enforced by the server and the query principal; never trusted from model text and never a filter Opus must remember to write. |
| `domain_id` | string | — | identifier | Constant 'corporate_cockpit' for every business artifact this runtime may reach. |
| `dataset_release_id` | string | — | identifier | Immutable release selection, pinned for the whole user request. Two releases are never mixed in one answer. |
| `reporting_quarter` | string | — | point_in_time | One of the twenty authorized anchor quarter labels, e.g. '2026Q2'. |
| `quarter_end_date` | date | — | point_in_time | Calendar end date of the reporting quarter. |
| `data_cutoff_at` | timestamp | — | point_in_time | Latest information timestamp permitted for this snapshot. Nothing published after it was knowable at it. |
| `source_system` | string | — | identifier | Actual source system, or the labelled synthetic generator. |
| `source_record_id` | string | — | identifier | Stable source-record identity, masked where required. |
| `source_period_start` | date | — | point_in_time | True start of the observed or financial-statement period. Not a guessed reporting date. |
| `source_period_end` | date | — | point_in_time | True end of the observed or financial-statement period. |
| `source_published_at` | timestamp | — | point_in_time | When the source published this observation. |
| `source_available_at` | timestamp | — | point_in_time | When this observation became available to the bank. Point-in-time control: an observation is only usable at a snapshot whose cutoff is at or after this. |
| `source_version` | string | — | identifier | Source definition version behind this value. |
| `mapping_version` | string | — | identifier | Transformation/mapping version applied on ingestion. |
| `ingested_at` | timestamp | — | point_in_time | Ingestion timestamp. |
| `provenance_id` | string | — | identifier | Permission-scoped lineage reference for this row. |
| `value_origin` | string | — | enum | How the value came to be. These are never conflated: a carried-forward annual statement is not a newly observed quarterly one, and a forecast is not an actual. |
| `missing_reason` | string | — | enum | Why a value is absent. Absent is not zero, and 'not_applicable' is not 'unknown'. |
| `currency_code` | string | — | enum | Original currency of the source amount. |
| `reporting_currency` | string | — | enum | The release's reporting currency. 'RCY' in a column name means this. |
| `fx_to_reporting_currency` | float | ratio | not_additive | Conversion scalar recorded for this snapshot. A stored scalar, not access to a separate FX domain. |
| `amount_scale` | string | — | enum | Unit, thousand, million. Normalized on ingestion; the source convention is preserved here. |
| `record_status` | string | — | enum | Whether this row is available, partial or not applicable. |
| `observation_age_days` | integer | days | not_additive | Age in days of the genuine observation behind this row, at the snapshot date. |
| `borrower_id` | string | — | identifier | Borrower identifier. |
| `rating_basis` | string | — | enum | Which rating and financial basis this row reports. |
| `risk_rating` | string | — | enum | Stored final internal rating on the declared nineteen-point scale. Cockpit reads it; Cockpit does not generate a new one. |
| `rating_rank` | integer | — | ordinal | Ordinal 1-19, where 1 is AAA and 19 is C. Larger is weaker. Not a probability, and differences between ranks are not calibrated distances. |
| `rating_scale_id` | string | — | identifier | The scale in force. |
| `rating_scale_version` | string | — | identifier | The mapping version in force. |
| `rating_effective_date` | date | — | point_in_time | When the rating took effect. |
| `rating_review_date` | date | — | point_in_time | When it is next due for review. |
| `rating_previous_recorded` | string | — | enum | The previous rating as RECORDED by the source, within authorized coverage. Not a reconstructed hidden history and not a lookup outside the twenty quarters. |
| `rating_at_origination` | string | — | enum | Origination rating where the source carries it as an attribute. It does not grant access to an additional snapshot. |
| `rating_outlook` | string | — | enum | Recorded outlook where present. |
| `rating_reason_recorded` | string | — | not_additive | The rationale AS RECORDED. Not a model-invented causal explanation. |
| `rating_override_flag` | boolean | — | enum | Whether the stored rating overrode a model output. |
| `rating_override_reason` | string | — | not_additive | The recorded override reason. Cockpit creates no new overrides. |
| `rating_source` | string | — | identifier | Permitted source reference. |
| `rating_approver_reference` | string | — | identifier | Approval reference, redacted as required. |
| `rating_status` | string | — | enum | Whether the rating was observed this quarter, carried forward, is missing, or failed to map from the source scale. An unmapped source rating is a mapping issue, never the closest-looking grade. |
| `rating_missing_reason` | string | — | enum | Why a rating is absent, where it is. |
| `current_ratio` | float | times | not_additive | Current assets / current liabilities. |
| `quick_ratio` | float | times | not_additive | (Eligible cash + short-term investments + net trade receivables) / current liabilities. Eligible cash excludes restricted cash; the exclusions are recorded in quick_ratio_basis. |
| `cash_ratio` | float | times | not_additive | (Eligible cash + short-term investments) / current liabilities. |
| `liquidity_ratio` | float | source_defined | not_additive | The bank's own liquidity ratio, whose definition is mandatory and is carried in liquidity_ratio_basis. Where it is identical to the current or cash ratio it is an ALIAS, not an independent signal. |
| `operating_cash_flow_to_current_liabilities` | float | times | not_additive | Operating cash flow / current liabilities. |
| `working_capital_to_total_assets` | float | fraction | not_additive | Working capital / total assets. |
| `liquid_assets_to_total_assets` | float | fraction | not_additive | Defined liquid assets / total assets. |
| `dscr` | float | times | not_additive | Cash available for debt service / matched debt service due. The bank's own basis is preserved in dscr_basis and is not replaced by a generic EBITDA-over-debt-service formula. |
| `interest_coverage_ratio` | float | times | not_additive | EBIT / gross interest expense. |
| `ebitda_interest_coverage` | float | times | not_additive | EBITDA / gross interest expense. |
| `fixed_charge_coverage_ratio` | float | times | not_additive | Source-defined fixed-charge coverage. The numerator add-backs and the lease and principal treatment are recorded in fixed_charge_coverage_basis. |
| `operating_cash_flow_to_debt` | float | times | not_additive | Operating cash flow / total debt. |
| `free_cash_flow_to_debt_service` | float | times | not_additive | Free cash flow / matched debt service due. |
| `net_debt_to_ebitda` | float | times | not_additive | Net debt / EBITDA on the stated period basis. A negative EBITDA makes this economically uninterpretable and it is flagged rather than hidden. |
| `debt_to_ebitda` | float | times | not_additive | Total debt / EBITDA on the stated period basis. |
| `debt_to_equity` | float | times | not_additive | Total debt / shareholders equity. |
| `liabilities_to_assets` | float | fraction | not_additive | Total liabilities / total assets. |
| `equity_to_assets` | float | fraction | not_additive | Shareholders equity / total assets. |
| `long_term_debt_to_capital` | float | fraction | not_additive | Long-term debt / (long-term debt + shareholders equity). |
| `tangible_net_worth_to_debt` | float | times | not_additive | Tangible net worth / total debt. |
| `gross_profit_margin` | float | fraction | not_additive | Gross profit / revenue. |
| `ebitda_margin` | float | fraction | not_additive | EBITDA / revenue. |
| `operating_profit_margin` | float | fraction | not_additive | EBIT / revenue. |
| `net_profit_margin` | float | fraction | not_additive | Net profit / revenue. |
| `return_on_assets` | float | fraction | not_additive | Net profit / average total assets, using the true opening balance. A PERIOD return unless explicitly annualized, which is stated in ratio_period_basis. |
| `return_on_equity` | float | fraction | not_additive | Net profit / average shareholders equity, on the same period rule. |
| `return_on_capital_employed` | float | fraction | not_additive | EBIT / average source-defined capital employed. |
| `operating_cash_flow_margin` | float | fraction | not_additive | Operating cash flow / revenue. |
| `free_cash_flow_margin` | float | fraction | not_additive | Free cash flow / revenue. |
| `total_asset_turnover` | float | times_per_period | not_additive | Revenue / average total assets. |
| `fixed_asset_turnover` | float | times_per_period | not_additive | Revenue / average net PPE. |
| `working_capital_turnover` | float | times_per_period | not_additive | Revenue / average working capital. |
| `inventory_turnover` | float | times_per_period | not_additive | Cost of goods sold / average inventory. |
| `receivables_turnover` | float | times_per_period | not_additive | Credit sales / average net trade receivables. Substituting total revenue for credit sales is a DIFFERENT ratio and is only permitted with an explicit alternate basis recorded. |
| `payables_turnover` | float | times_per_period | not_additive | Credit purchases / average trade payables. Substituting cost of sales is likewise a different ratio requiring an explicit basis. |
| `receivables_days` | float | days | not_additive | Average net receivables / credit sales x matched period days. |
| `inventory_days` | float | days | not_additive | Average inventory / cost of goods sold x matched period days. |
| `payables_days` | float | days | not_additive | Average trade payables / credit purchases x matched period days. |
| `cash_conversion_cycle_days` | float | days | not_additive | Receivables days + inventory days - payables days, all on the same period and basis. |
| `capex_to_operating_cash_flow` | float | times | not_additive | Capital expenditure / operating cash flow. |
| `current_ratio_source_value` | float | times | not_additive | The value as SUPPLIED by the source for current_ratio, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `quick_ratio_source_value` | float | times | not_additive | The value as SUPPLIED by the source for quick_ratio, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `cash_ratio_source_value` | float | times | not_additive | The value as SUPPLIED by the source for cash_ratio, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `liquidity_ratio_source_value` | float | source_defined | not_additive | The value as SUPPLIED by the source for liquidity_ratio, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `operating_cash_flow_to_current_liabilities_source_value` | float | times | not_additive | The value as SUPPLIED by the source for operating_cash_flow_to_current_liabilities, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `working_capital_to_total_assets_source_value` | float | fraction | not_additive | The value as SUPPLIED by the source for working_capital_to_total_assets, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `liquid_assets_to_total_assets_source_value` | float | fraction | not_additive | The value as SUPPLIED by the source for liquid_assets_to_total_assets, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `dscr_source_value` | float | times | not_additive | The value as SUPPLIED by the source for dscr, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `interest_coverage_ratio_source_value` | float | times | not_additive | The value as SUPPLIED by the source for interest_coverage_ratio, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `ebitda_interest_coverage_source_value` | float | times | not_additive | The value as SUPPLIED by the source for ebitda_interest_coverage, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `fixed_charge_coverage_ratio_source_value` | float | times | not_additive | The value as SUPPLIED by the source for fixed_charge_coverage_ratio, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `operating_cash_flow_to_debt_source_value` | float | times | not_additive | The value as SUPPLIED by the source for operating_cash_flow_to_debt, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `free_cash_flow_to_debt_service_source_value` | float | times | not_additive | The value as SUPPLIED by the source for free_cash_flow_to_debt_service, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `net_debt_to_ebitda_source_value` | float | times | not_additive | The value as SUPPLIED by the source for net_debt_to_ebitda, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `debt_to_ebitda_source_value` | float | times | not_additive | The value as SUPPLIED by the source for debt_to_ebitda, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `debt_to_equity_source_value` | float | times | not_additive | The value as SUPPLIED by the source for debt_to_equity, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `liabilities_to_assets_source_value` | float | fraction | not_additive | The value as SUPPLIED by the source for liabilities_to_assets, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `equity_to_assets_source_value` | float | fraction | not_additive | The value as SUPPLIED by the source for equity_to_assets, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `long_term_debt_to_capital_source_value` | float | fraction | not_additive | The value as SUPPLIED by the source for long_term_debt_to_capital, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `tangible_net_worth_to_debt_source_value` | float | times | not_additive | The value as SUPPLIED by the source for tangible_net_worth_to_debt, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `gross_profit_margin_source_value` | float | fraction | not_additive | The value as SUPPLIED by the source for gross_profit_margin, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `ebitda_margin_source_value` | float | fraction | not_additive | The value as SUPPLIED by the source for ebitda_margin, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `operating_profit_margin_source_value` | float | fraction | not_additive | The value as SUPPLIED by the source for operating_profit_margin, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `net_profit_margin_source_value` | float | fraction | not_additive | The value as SUPPLIED by the source for net_profit_margin, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `return_on_assets_source_value` | float | fraction | not_additive | The value as SUPPLIED by the source for return_on_assets, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `return_on_equity_source_value` | float | fraction | not_additive | The value as SUPPLIED by the source for return_on_equity, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `return_on_capital_employed_source_value` | float | fraction | not_additive | The value as SUPPLIED by the source for return_on_capital_employed, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `operating_cash_flow_margin_source_value` | float | fraction | not_additive | The value as SUPPLIED by the source for operating_cash_flow_margin, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `free_cash_flow_margin_source_value` | float | fraction | not_additive | The value as SUPPLIED by the source for free_cash_flow_margin, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `total_asset_turnover_source_value` | float | times_per_period | not_additive | The value as SUPPLIED by the source for total_asset_turnover, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `fixed_asset_turnover_source_value` | float | times_per_period | not_additive | The value as SUPPLIED by the source for fixed_asset_turnover, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `working_capital_turnover_source_value` | float | times_per_period | not_additive | The value as SUPPLIED by the source for working_capital_turnover, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `inventory_turnover_source_value` | float | times_per_period | not_additive | The value as SUPPLIED by the source for inventory_turnover, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `receivables_turnover_source_value` | float | times_per_period | not_additive | The value as SUPPLIED by the source for receivables_turnover, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `payables_turnover_source_value` | float | times_per_period | not_additive | The value as SUPPLIED by the source for payables_turnover, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `receivables_days_source_value` | float | days | not_additive | The value as SUPPLIED by the source for receivables_days, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `inventory_days_source_value` | float | days | not_additive | The value as SUPPLIED by the source for inventory_days, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `payables_days_source_value` | float | days | not_additive | The value as SUPPLIED by the source for payables_days, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `cash_conversion_cycle_days_source_value` | float | days | not_additive | The value as SUPPLIED by the source for cash_conversion_cycle_days, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `capex_to_operating_cash_flow_source_value` | float | times | not_additive | The value as SUPPLIED by the source for capex_to_operating_cash_flow, where the source supplies one. Kept apart from the transparently derived value so the two can be compared rather than silently merged. |
| `current_ratio_status` | string | — | enum | Whether current_ratio is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `quick_ratio_status` | string | — | enum | Whether quick_ratio is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `cash_ratio_status` | string | — | enum | Whether cash_ratio is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `liquidity_ratio_status` | string | — | enum | Whether liquidity_ratio is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `operating_cash_flow_to_current_liabilities_status` | string | — | enum | Whether operating_cash_flow_to_current_liabilities is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `working_capital_to_total_assets_status` | string | — | enum | Whether working_capital_to_total_assets is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `liquid_assets_to_total_assets_status` | string | — | enum | Whether liquid_assets_to_total_assets is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `dscr_status` | string | — | enum | Whether dscr is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `interest_coverage_ratio_status` | string | — | enum | Whether interest_coverage_ratio is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `ebitda_interest_coverage_status` | string | — | enum | Whether ebitda_interest_coverage is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `fixed_charge_coverage_ratio_status` | string | — | enum | Whether fixed_charge_coverage_ratio is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `operating_cash_flow_to_debt_status` | string | — | enum | Whether operating_cash_flow_to_debt is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `free_cash_flow_to_debt_service_status` | string | — | enum | Whether free_cash_flow_to_debt_service is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `net_debt_to_ebitda_status` | string | — | enum | Whether net_debt_to_ebitda is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `debt_to_ebitda_status` | string | — | enum | Whether debt_to_ebitda is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `debt_to_equity_status` | string | — | enum | Whether debt_to_equity is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `liabilities_to_assets_status` | string | — | enum | Whether liabilities_to_assets is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `equity_to_assets_status` | string | — | enum | Whether equity_to_assets is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `long_term_debt_to_capital_status` | string | — | enum | Whether long_term_debt_to_capital is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `tangible_net_worth_to_debt_status` | string | — | enum | Whether tangible_net_worth_to_debt is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `gross_profit_margin_status` | string | — | enum | Whether gross_profit_margin is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `ebitda_margin_status` | string | — | enum | Whether ebitda_margin is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `operating_profit_margin_status` | string | — | enum | Whether operating_profit_margin is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `net_profit_margin_status` | string | — | enum | Whether net_profit_margin is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `return_on_assets_status` | string | — | enum | Whether return_on_assets is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `return_on_equity_status` | string | — | enum | Whether return_on_equity is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `return_on_capital_employed_status` | string | — | enum | Whether return_on_capital_employed is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `operating_cash_flow_margin_status` | string | — | enum | Whether operating_cash_flow_margin is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `free_cash_flow_margin_status` | string | — | enum | Whether free_cash_flow_margin is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `total_asset_turnover_status` | string | — | enum | Whether total_asset_turnover is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `fixed_asset_turnover_status` | string | — | enum | Whether fixed_asset_turnover is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `working_capital_turnover_status` | string | — | enum | Whether working_capital_turnover is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `inventory_turnover_status` | string | — | enum | Whether inventory_turnover is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `receivables_turnover_status` | string | — | enum | Whether receivables_turnover is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `payables_turnover_status` | string | — | enum | Whether payables_turnover is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `receivables_days_status` | string | — | enum | Whether receivables_days is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `inventory_days_status` | string | — | enum | Whether inventory_days is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `payables_days_status` | string | — | enum | Whether payables_days is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `cash_conversion_cycle_days_status` | string | — | enum | Whether cash_conversion_cycle_days is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `capex_to_operating_cash_flow_status` | string | — | enum | Whether capex_to_operating_cash_flow is observed, derived, unavailable because an input is missing, or invalid because its denominator is zero or negative. Division by zero yields 'unavailable', never infinity or zero. |
| `ratio_definition_id` | string | — | identifier | Which ratio definition set these values follow. |
| `ratio_period_basis` | string | — | enum | The financial period the ratio flows are measured over, and whether a return ratio has been annualized. |
| `ratio_period_days` | integer | days | not_additive | Matched period days used by the day-based ratios. |
| `quick_ratio_basis` | string | — | identifier | Which assets the quick ratio counts and what it excludes. |
| `liquidity_ratio_basis` | string | — | identifier | The bank's liquidity-ratio definition. Mandatory: without it the value cannot be interpreted or compared. |
| `fixed_charge_coverage_basis` | string | — | identifier | Numerator add-backs and lease/principal treatment for fixed-charge coverage. |
| `receivables_turnover_basis` | string | — | enum | Whether credit sales or total revenue was used. |
| `payables_turnover_basis` | string | — | enum | Whether credit purchases or cost of sales was used. |

### `cockpit_qualitative_quarter` — The twenty qualitative questions

**Grain:** borrower_id x reporting_quarter x question_id -- twenty rows per borrower-quarter when fully answered

33 columns.

| Column | Type | Unit | Aggregation | Definition |
|---|---|---|---|---|
| `tenant_id` | string | — | identifier | Authenticated data owner. Enforced by the server and the query principal; never trusted from model text and never a filter Opus must remember to write. |
| `domain_id` | string | — | identifier | Constant 'corporate_cockpit' for every business artifact this runtime may reach. |
| `dataset_release_id` | string | — | identifier | Immutable release selection, pinned for the whole user request. Two releases are never mixed in one answer. |
| `reporting_quarter` | string | — | point_in_time | One of the twenty authorized anchor quarter labels, e.g. '2026Q2'. |
| `quarter_end_date` | date | — | point_in_time | Calendar end date of the reporting quarter. |
| `data_cutoff_at` | timestamp | — | point_in_time | Latest information timestamp permitted for this snapshot. Nothing published after it was knowable at it. |
| `source_system` | string | — | identifier | Actual source system, or the labelled synthetic generator. |
| `source_record_id` | string | — | identifier | Stable source-record identity, masked where required. |
| `source_period_start` | date | — | point_in_time | True start of the observed or financial-statement period. Not a guessed reporting date. |
| `source_period_end` | date | — | point_in_time | True end of the observed or financial-statement period. |
| `source_published_at` | timestamp | — | point_in_time | When the source published this observation. |
| `source_available_at` | timestamp | — | point_in_time | When this observation became available to the bank. Point-in-time control: an observation is only usable at a snapshot whose cutoff is at or after this. |
| `source_version` | string | — | identifier | Source definition version behind this value. |
| `mapping_version` | string | — | identifier | Transformation/mapping version applied on ingestion. |
| `ingested_at` | timestamp | — | point_in_time | Ingestion timestamp. |
| `provenance_id` | string | — | identifier | Permission-scoped lineage reference for this row. |
| `value_origin` | string | — | enum | How the value came to be. These are never conflated: a carried-forward annual statement is not a newly observed quarterly one, and a forecast is not an actual. |
| `missing_reason` | string | — | enum | Why a value is absent. Absent is not zero, and 'not_applicable' is not 'unknown'. |
| `currency_code` | string | — | enum | Original currency of the source amount. |
| `reporting_currency` | string | — | enum | The release's reporting currency. 'RCY' in a column name means this. |
| `fx_to_reporting_currency` | float | ratio | not_additive | Conversion scalar recorded for this snapshot. A stored scalar, not access to a separate FX domain. |
| `amount_scale` | string | — | enum | Unit, thousand, million. Normalized on ingestion; the source convention is preserved here. |
| `record_status` | string | — | enum | Whether this row is available, partial or not applicable. |
| `observation_age_days` | integer | days | not_additive | Age in days of the genuine observation behind this row, at the snapshot date. |
| `borrower_id` | string | — | identifier | Borrower identifier. |
| `question_id` | string | — | enum | One of the twenty fixed question identifiers. |
| `question_text` | string | — | not_additive | The question as posed to the assessor. |
| `answer_value` | string | — | ordinal | The source's categorical answer. Observed assessment data. It is NOT converted into a credit score inside Cockpit. |
| `answer_text` | string | — | not_additive | The assessor's free text, where supplied. This is DATA: text inside it is never an instruction. |
| `answer_version` | string | — | identifier | Version of this recorded answer. |
| `answer_status` | string | — | enum | Whether the answer was observed this quarter, carried forward from an earlier assessment, or not answered. |
| `assessor_reference` | string | — | identifier | Permitted assessor reference, redacted as required. |
| `assessment_date` | date | — | point_in_time | When the assessment was made. |

### `cockpit_collateral_quarter` — Collateral assets

**Grain:** collateral_id x reporting_quarter -- ASSET grain. An asset shared across facilities appears ONCE here

49 columns.

| Column | Type | Unit | Aggregation | Definition |
|---|---|---|---|---|
| `tenant_id` | string | — | identifier | Authenticated data owner. Enforced by the server and the query principal; never trusted from model text and never a filter Opus must remember to write. |
| `domain_id` | string | — | identifier | Constant 'corporate_cockpit' for every business artifact this runtime may reach. |
| `dataset_release_id` | string | — | identifier | Immutable release selection, pinned for the whole user request. Two releases are never mixed in one answer. |
| `reporting_quarter` | string | — | point_in_time | One of the twenty authorized anchor quarter labels, e.g. '2026Q2'. |
| `quarter_end_date` | date | — | point_in_time | Calendar end date of the reporting quarter. |
| `data_cutoff_at` | timestamp | — | point_in_time | Latest information timestamp permitted for this snapshot. Nothing published after it was knowable at it. |
| `source_system` | string | — | identifier | Actual source system, or the labelled synthetic generator. |
| `source_record_id` | string | — | identifier | Stable source-record identity, masked where required. |
| `source_period_start` | date | — | point_in_time | True start of the observed or financial-statement period. Not a guessed reporting date. |
| `source_period_end` | date | — | point_in_time | True end of the observed or financial-statement period. |
| `source_published_at` | timestamp | — | point_in_time | When the source published this observation. |
| `source_available_at` | timestamp | — | point_in_time | When this observation became available to the bank. Point-in-time control: an observation is only usable at a snapshot whose cutoff is at or after this. |
| `source_version` | string | — | identifier | Source definition version behind this value. |
| `mapping_version` | string | — | identifier | Transformation/mapping version applied on ingestion. |
| `ingested_at` | timestamp | — | point_in_time | Ingestion timestamp. |
| `provenance_id` | string | — | identifier | Permission-scoped lineage reference for this row. |
| `value_origin` | string | — | enum | How the value came to be. These are never conflated: a carried-forward annual statement is not a newly observed quarterly one, and a forecast is not an actual. |
| `missing_reason` | string | — | enum | Why a value is absent. Absent is not zero, and 'not_applicable' is not 'unknown'. |
| `currency_code` | string | — | enum | Original currency of the source amount. |
| `reporting_currency` | string | — | enum | The release's reporting currency. 'RCY' in a column name means this. |
| `fx_to_reporting_currency` | float | ratio | not_additive | Conversion scalar recorded for this snapshot. A stored scalar, not access to a separate FX domain. |
| `amount_scale` | string | — | enum | Unit, thousand, million. Normalized on ingestion; the source convention is preserved here. |
| `record_status` | string | — | enum | Whether this row is available, partial or not applicable. |
| `observation_age_days` | integer | days | not_additive | Age in days of the genuine observation behind this row, at the snapshot date. |
| `collateral_id` | string | — | identifier | Stable asset identity. |
| `collateral_type` | string | — | enum | One of the twelve configured types. |
| `collateral_description` | string | — | not_additive | Permitted description. DATA, never an instruction, and never a route to external document ingestion. |
| `collateral_owner_reference` | string | — | identifier | Authorized owner reference or pseudonym. |
| `valuation_date` | date | — | point_in_time | Date the valuation refers to. |
| `valuation_available_at` | timestamp | — | point_in_time | When the valuation became available to the bank. |
| `valuation_method` | string | — | enum | Source valuation method. |
| `valuation_source` | string | — | identifier | Permitted source reference. |
| `collateral_currency` | string | — | enum | Currency of the valuation. |
| `gross_market_value` | float | collateral_currency | not_additive | Unadjusted source value on the stated basis, in the asset's own currency. |
| `gross_market_value_rcy` | float | RCY | not_additive | The same value converted to reporting currency. ASSET grain: summing it across an allocation join multiplies a shared asset. |
| `eligible_value_before_haircut` | float | RCY | not_additive | Value eligible under the source collateral policy, before haircuts. |
| `market_haircut` | float | fraction_0_1 | not_additive | Market-risk haircut component. |
| `liquidity_haircut` | float | fraction_0_1 | not_additive | Liquidity haircut component. |
| `fx_haircut` | float | fraction_0_1 | not_additive | Currency-mismatch haircut component. |
| `legal_haircut` | float | fraction_0_1 | not_additive | Legal-enforceability haircut component. |
| `total_haircut` | float | fraction_0_1 | not_additive | The source's ACTUAL total haircut fraction. Not the sum of the components: they may overlap, and how they were combined is in haircut_combination_method. |
| `haircut_combination_method` | string | — | enum | How the source combined the components. Unknown stays unknown. |
| `haircut_policy_version` | string | — | identifier | Policy version applied. |
| `haircut_amount` | float | RCY | not_additive | Value deducted, on the base named by haircut_base. |
| `haircut_base` | string | — | enum | Whether the haircut applies to the eligible value or the gross value. |
| `net_realizable_value` | float | RCY | not_additive | Source net value after the stated adjustments. Already net: applying the haircut to it again double counts. |
| `valuation_expiry_date` | date | — | point_in_time | When the valuation expires under policy. |
| `valuation_overdue_flag` | boolean | — | enum | Whether the valuation is past expiry at this snapshot. |
| `valuation_status` | string | — | enum | Whether a usable valuation exists. An asset present with no valuation is recorded here, not dropped. |

### `cockpit_collateral_allocation` — Collateral allocation to positions

**Grain:** collateral_id x facility_id x position_id x reporting_quarter -- the allocation link. Summing gross_market_value across this relation double counts a shared asset

34 columns.

| Column | Type | Unit | Aggregation | Definition |
|---|---|---|---|---|
| `tenant_id` | string | — | identifier | Authenticated data owner. Enforced by the server and the query principal; never trusted from model text and never a filter Opus must remember to write. |
| `domain_id` | string | — | identifier | Constant 'corporate_cockpit' for every business artifact this runtime may reach. |
| `dataset_release_id` | string | — | identifier | Immutable release selection, pinned for the whole user request. Two releases are never mixed in one answer. |
| `reporting_quarter` | string | — | point_in_time | One of the twenty authorized anchor quarter labels, e.g. '2026Q2'. |
| `quarter_end_date` | date | — | point_in_time | Calendar end date of the reporting quarter. |
| `data_cutoff_at` | timestamp | — | point_in_time | Latest information timestamp permitted for this snapshot. Nothing published after it was knowable at it. |
| `source_system` | string | — | identifier | Actual source system, or the labelled synthetic generator. |
| `source_record_id` | string | — | identifier | Stable source-record identity, masked where required. |
| `source_period_start` | date | — | point_in_time | True start of the observed or financial-statement period. Not a guessed reporting date. |
| `source_period_end` | date | — | point_in_time | True end of the observed or financial-statement period. |
| `source_published_at` | timestamp | — | point_in_time | When the source published this observation. |
| `source_available_at` | timestamp | — | point_in_time | When this observation became available to the bank. Point-in-time control: an observation is only usable at a snapshot whose cutoff is at or after this. |
| `source_version` | string | — | identifier | Source definition version behind this value. |
| `mapping_version` | string | — | identifier | Transformation/mapping version applied on ingestion. |
| `ingested_at` | timestamp | — | point_in_time | Ingestion timestamp. |
| `provenance_id` | string | — | identifier | Permission-scoped lineage reference for this row. |
| `value_origin` | string | — | enum | How the value came to be. These are never conflated: a carried-forward annual statement is not a newly observed quarterly one, and a forecast is not an actual. |
| `missing_reason` | string | — | enum | Why a value is absent. Absent is not zero, and 'not_applicable' is not 'unknown'. |
| `currency_code` | string | — | enum | Original currency of the source amount. |
| `reporting_currency` | string | — | enum | The release's reporting currency. 'RCY' in a column name means this. |
| `fx_to_reporting_currency` | float | ratio | not_additive | Conversion scalar recorded for this snapshot. A stored scalar, not access to a separate FX domain. |
| `amount_scale` | string | — | enum | Unit, thousand, million. Normalized on ingestion; the source convention is preserved here. |
| `record_status` | string | — | enum | Whether this row is available, partial or not applicable. |
| `observation_age_days` | integer | days | not_additive | Age in days of the genuine observation behind this row, at the snapshot date. |
| `allocation_id` | string | — | identifier | Source allocation identity. |
| `collateral_id` | string | — | identifier | The asset allocated. |
| `facility_id` | string | — | identifier | The secured facility. |
| `position_id` | string | — | identifier | The secured position. |
| `allocation_share` | float | fraction_0_1 | not_additive | The source share of the asset attributed to this position. Shares for one asset sum to at most one; Cockpit does not invent an allocation where the source has none. |
| `allocated_gross_value_rcy` | float | RCY | additive | Gross value attributable to this position. Additive across positions. |
| `allocated_net_value_rcy` | float | RCY | additive | Net value attributable to this position, after haircuts. Additive. |
| `lien_rank` | integer | — | ordinal | Source lien priority. 1 is first-ranking. |
| `secured_amount` | float | RCY | additive | Source secured amount for this position. |
| `allocation_status` | string | — | enum | Whether the allocation is a source figure, derived from a stated rule, or unavailable. |

### `cockpit_covenant_quarter` — Covenants and their tests

**Grain:** covenant_id x reporting_quarter x test_version -- obligation grain. A borrower-wide covenant must not be counted once per facility

59 columns.

| Column | Type | Unit | Aggregation | Definition |
|---|---|---|---|---|
| `tenant_id` | string | — | identifier | Authenticated data owner. Enforced by the server and the query principal; never trusted from model text and never a filter Opus must remember to write. |
| `domain_id` | string | — | identifier | Constant 'corporate_cockpit' for every business artifact this runtime may reach. |
| `dataset_release_id` | string | — | identifier | Immutable release selection, pinned for the whole user request. Two releases are never mixed in one answer. |
| `reporting_quarter` | string | — | point_in_time | One of the twenty authorized anchor quarter labels, e.g. '2026Q2'. |
| `quarter_end_date` | date | — | point_in_time | Calendar end date of the reporting quarter. |
| `data_cutoff_at` | timestamp | — | point_in_time | Latest information timestamp permitted for this snapshot. Nothing published after it was knowable at it. |
| `source_system` | string | — | identifier | Actual source system, or the labelled synthetic generator. |
| `source_record_id` | string | — | identifier | Stable source-record identity, masked where required. |
| `source_period_start` | date | — | point_in_time | True start of the observed or financial-statement period. Not a guessed reporting date. |
| `source_period_end` | date | — | point_in_time | True end of the observed or financial-statement period. |
| `source_published_at` | timestamp | — | point_in_time | When the source published this observation. |
| `source_available_at` | timestamp | — | point_in_time | When this observation became available to the bank. Point-in-time control: an observation is only usable at a snapshot whose cutoff is at or after this. |
| `source_version` | string | — | identifier | Source definition version behind this value. |
| `mapping_version` | string | — | identifier | Transformation/mapping version applied on ingestion. |
| `ingested_at` | timestamp | — | point_in_time | Ingestion timestamp. |
| `provenance_id` | string | — | identifier | Permission-scoped lineage reference for this row. |
| `value_origin` | string | — | enum | How the value came to be. These are never conflated: a carried-forward annual statement is not a newly observed quarterly one, and a forecast is not an actual. |
| `missing_reason` | string | — | enum | Why a value is absent. Absent is not zero, and 'not_applicable' is not 'unknown'. |
| `currency_code` | string | — | enum | Original currency of the source amount. |
| `reporting_currency` | string | — | enum | The release's reporting currency. 'RCY' in a column name means this. |
| `fx_to_reporting_currency` | float | ratio | not_additive | Conversion scalar recorded for this snapshot. A stored scalar, not access to a separate FX domain. |
| `amount_scale` | string | — | enum | Unit, thousand, million. Normalized on ingestion; the source convention is preserved here. |
| `record_status` | string | — | enum | Whether this row is available, partial or not applicable. |
| `observation_age_days` | integer | days | not_additive | Age in days of the genuine observation behind this row, at the snapshot date. |
| `covenant_id` | string | — | identifier | Stable contractual obligation identity. |
| `borrower_id` | string | — | identifier | The bound borrower. |
| `facility_id` | string | — | identifier | The bound facility, where the obligation is facility-specific. Null for a borrower-wide obligation. |
| `binding_scope` | string | — | enum | Whether the obligation binds the borrower as a whole or one facility. A borrower-wide covenant joined to facilities is counted once per facility unless this is respected. |
| `covenant_name` | string | — | not_additive | The obligation's name. |
| `covenant_type` | string | — | enum | Financial or non-financial obligation. |
| `metric_name` | string | — | enum | The exact metric tested, e.g. the source-defined DSCR. |
| `metric_definition_id` | string | — | identifier | Which definition of that metric the contract uses. A covenant DSCR and a reporting DSCR need not be the same measure. |
| `contract_reference` | string | — | identifier | Permitted contractual reference. |
| `effective_date` | date | — | point_in_time | Start of contractual validity. |
| `expiry_date` | date | — | point_in_time | End of contractual validity. |
| `test_frequency` | string | — | enum | Contractual test frequency. |
| `test_due_date` | date | — | point_in_time | When this test was contractually due. |
| `test_period_start` | date | — | point_in_time | Start of the observed test period. |
| `test_period_end` | date | — | point_in_time | End of the observed test period. |
| `comparison_operator` | string | — | enum | The contractual comparator. Headroom direction follows from it: for '>=' headroom is observed minus threshold, for '<=' it is threshold minus observed. |
| `threshold_value` | float | — | not_additive | The contractual limit. Null where the rule is a range or categorical. |
| `threshold_lower` | float | — | not_additive | Lower limit for a range test. |
| `threshold_upper` | float | — | not_additive | Upper limit for a range test. |
| `threshold_unit` | string | — | enum | The unit and period basis of the threshold. It must match the tested measure's, or the comparison is meaningless. |
| `observed_value` | float | — | not_additive | The numeric observation tested. |
| `observed_text` | string | — | not_additive | The categorical observation, where the test is not numeric. |
| `test_status` | string | — | enum | The recorded outcome. 'not_tested' and 'overdue' are NOT compliance: an untested covenant is never reported as compliant. |
| `headroom_value` | float | — | not_additive | Stored or transparently derived headroom, in threshold_unit, signed so that a positive value is compliant under the actual comparator. |
| `headroom_unit` | string | — | enum | Unit of the headroom figure. |
| `breach_date` | date | — | point_in_time | Observed breach date. |
| `breach_reason_recorded` | string | — | not_additive | The recorded breach reason. Not a model-invented cause. |
| `waiver_flag` | boolean | — | enum | Whether a waiver was granted. |
| `waiver_date` | date | — | point_in_time | When the waiver was granted. |
| `waiver_expiry_date` | date | — | point_in_time | When the waiver lapses. |
| `cure_deadline` | date | — | point_in_time | Contractual cure deadline. |
| `cure_status` | string | — | enum | Recorded cure status. |
| `evidence_reference` | string | — | identifier | Permitted provenance reference. |
| `test_version` | string | — | identifier | Version of this recorded test. |
| `test_missing_reason` | string | — | enum | Why a test was not performed. Recorded so that untested is never reported as compliant. |

### `cockpit_macro_quarter_window` — Macroeconomic window (the second time axis)

**Grain:** reporting_quarter (anchor) x factor_id x country_or_region x scenario_id x quarter_offset (-4..+15) -- 20 offsets per anchor

40 columns.

| Column | Type | Unit | Aggregation | Definition |
|---|---|---|---|---|
| `tenant_id` | string | — | identifier | Authenticated data owner. Enforced by the server and the query principal; never trusted from model text and never a filter Opus must remember to write. |
| `domain_id` | string | — | identifier | Constant 'corporate_cockpit' for every business artifact this runtime may reach. |
| `dataset_release_id` | string | — | identifier | Immutable release selection, pinned for the whole user request. Two releases are never mixed in one answer. |
| `reporting_quarter` | string | — | point_in_time | One of the twenty authorized anchor quarter labels, e.g. '2026Q2'. |
| `quarter_end_date` | date | — | point_in_time | Calendar end date of the reporting quarter. |
| `data_cutoff_at` | timestamp | — | point_in_time | Latest information timestamp permitted for this snapshot. Nothing published after it was knowable at it. |
| `source_system` | string | — | identifier | Actual source system, or the labelled synthetic generator. |
| `source_record_id` | string | — | identifier | Stable source-record identity, masked where required. |
| `source_period_start` | date | — | point_in_time | True start of the observed or financial-statement period. Not a guessed reporting date. |
| `source_period_end` | date | — | point_in_time | True end of the observed or financial-statement period. |
| `source_published_at` | timestamp | — | point_in_time | When the source published this observation. |
| `source_available_at` | timestamp | — | point_in_time | When this observation became available to the bank. Point-in-time control: an observation is only usable at a snapshot whose cutoff is at or after this. |
| `source_version` | string | — | identifier | Source definition version behind this value. |
| `mapping_version` | string | — | identifier | Transformation/mapping version applied on ingestion. |
| `ingested_at` | timestamp | — | point_in_time | Ingestion timestamp. |
| `provenance_id` | string | — | identifier | Permission-scoped lineage reference for this row. |
| `value_origin` | string | — | enum | How the value came to be. These are never conflated: a carried-forward annual statement is not a newly observed quarterly one, and a forecast is not an actual. |
| `missing_reason` | string | — | enum | Why a value is absent. Absent is not zero, and 'not_applicable' is not 'unknown'. |
| `currency_code` | string | — | enum | Original currency of the source amount. |
| `reporting_currency` | string | — | enum | The release's reporting currency. 'RCY' in a column name means this. |
| `fx_to_reporting_currency` | float | ratio | not_additive | Conversion scalar recorded for this snapshot. A stored scalar, not access to a separate FX domain. |
| `amount_scale` | string | — | enum | Unit, thousand, million. Normalized on ingestion; the source convention is preserved here. |
| `record_status` | string | — | enum | Whether this row is available, partial or not applicable. |
| `observation_age_days` | integer | days | not_additive | Age in days of the genuine observation behind this row, at the snapshot date. |
| `factor_id` | string | — | enum | One of the ten configured factors. |
| `macro_target_quarter` | string | — | point_in_time | The quarter this value DESCRIBES. Distinct from reporting_quarter, which is the anchor it was known at. A target beyond the reporting calendar is a forecast horizon, not a twenty-first reporting snapshot. |
| `quarter_offset` | integer | — | ordinal | macro_target_quarter minus reporting_quarter, in quarters. Runs -4 through +15 inclusive: twenty positions per anchor. |
| `country_or_region` | string | — | enum | The geography this value applies to. Joins to a facility's country_code. |
| `scenario_id` | string | — | enum | An ALREADY STORED source scenario. Cockpit cannot create one. |
| `value` | float | — | not_additive | The factor value, in `unit`. |
| `unit` | string | — | enum | The value's unit. |
| `index_base_period` | string | — | not_additive | Base period for an index factor. Without it an index level cannot be compared across sources. |
| `frequency` | string | — | enum | Native source frequency. |
| `quarter_aggregation_method` | string | — | enum | How a non-quarterly source was aggregated to the quarter. |
| `observation_status` | string | — | enum | Whether this value is a historical actual, a current actual, a nowcast for a quarter not yet published, or a forecast. A forward value is NEVER an actual. |
| `forecast_vintage` | string | — | identifier | The vintage of the forecast round this value came from. A later actual does not overwrite an earlier forecast as though it had been known then. |
| `published_at` | timestamp | — | point_in_time | When the source published this value. |
| `available_at` | timestamp | — | point_in_time | When it became available. A snapshot may use only values available by its data_cutoff_at. |
| `source_name` | string | — | identifier | The macro source. |
| `source_reference` | string | — | identifier | Permitted source reference. |

### `cockpit_macro_pivot` — Macroeconomic convenience pivot

**Grain:** one row per anchor, geography and scenario

200 columns.

Generated as `<factor_id>_<offset_suffix>` for each of the ten factors and each of the twenty offsets `lag4`…`lag1`, `current`, `lead1`…`lead15`. Two hundred value CELLS per row, not two hundred factors, and they add no reporting quarter. Every one resolves through the catalogue.

