"""
CreditProbe's governed agentic layer.

The principle the whole package is built to hold:

    Agents may propose, coordinate and interpret.
    CreditProbe's governed runtime must calculate and prove.
    Humans must approve material side effects.

Nothing in here computes a credit figure. Every number an agent reports came
out of the deterministic Analytical Runtime through the Tool Registry, carries
a plan fingerprint, and reconciles the same way it would have if a person had
asked for it directly. What the agents add is *coordination*: deciding which
governed questions are worth asking, asking several of them at once, checking
the answers against each other, and saying what the set of them means.
"""
